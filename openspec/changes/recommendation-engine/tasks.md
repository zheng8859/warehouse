> 排序说明：rules 要求「按关键路径 `F1 → F2 → F9 → F3 → F4 → …` 排序」。本变更只占 **F3 / F4** 两环，
> 其前驱 F1 / F2 / F9 属阶段四，**不在本变更内**（见第 10 组「不适用」登记）。故组内按 F3 → F4 的数据流排序：
> 因子 → 可行集/评分 → 队列序 → 降级链 → 主循环 → 理由 → 端点。
>
> 验证方式的约定：本阶段的验证一律是 `backend/tests/` 下的 pytest（`00` §3.1 的「逻辑测试保留完整代码」）。
> `evals/` 目录属阶段六（`CLAUDE.md` §二：现在跑刻意以退出码 3 失败），故每条任务标注的是它**对应**的
> `20` §「基线层」评测场景编号（`SC-` / `AC-`），到阶段六落成 evals 用例时按此映射取数。

## 1. 前置：契约、常量与测试夹具

- [x] 1.1 在 `backend/app/schemas/reason.py` 落 `17` §10.1 / §10.7 的 Pydantic DTO（请求 `BatchAllocateRequest`、响应 `BatchAllocateResponse`、理由 `ReasonPayload`），字段名逐个对照 `17` §10.1 与 §10.7 的表。验证：`tests/` 新增 schema 用例，断言「六因子键齐全」「`breakdown` 为 巷道 × 因子 二层结构」「`degraded_alerts` 每项含 `job_order_id` / `aisle` / `message`」；`POST /openapi.json` 能生成该模型
- [x] 1.2 在 `backend/app/engine/__init__.py` 导出 7 个模块的公共入口，并把 D14 的四个**待确认默认值**（N、优先级三项权重、换算恒等、档位留空）集中成具名常量（含 `TODO` 指向 `design.md` D14 的表格行）。验证：`tests/logic/` 断言常量存在且六因子名与 `app/models/configuration.py:WEIGHT_FACTORS` **逐项相等**（防两处各写一份而漂移）。**实施补记（2026-09-12）**：① `engine/__init__.py` 成为包入口 —— 七个模块的公共名**逐项再导出**，且**只搬运、不包装**（没有一层同名转发函数：转发层会让 `grep` 到的定义点与真正的定义点错位）。`__all__` = 七模块 `__all__` 的**并集** + `WEIGHT_FACTORS`（59 名）；顺带补上 `factors.py` 的 `__all__`（七个模块里只有它此前没有）。② 四个待确认默认值：**集中的是可见性，不复制值** —— D14 的「落地位置」列已指定各自的家（`priority.py` 的两项、`allocator.to_occupied_cells()` 的函数体、`degradation.resolve_tiers()` 的解析），故包入口只再导出，并在文件末尾列四条 `TODO(design.md D14 表格第 N 行)` 作为「还欠业务方一个数」的全量清单。③ 为此**新落一个常量** `priority.DEFAULT_OUTBOUND_WINDOW_DAYS`（D14 表格第 1 行的「`priority.py` 的一个常量」），取值 **`None`**（= 留空）：`08` / `14` §3.2 / PRD US-04 全库都只写符号 `N`、无一处给数，随手填一个天数会立刻成为**未经确认的业务口径**并被当成验收依据；`None` 与 D5「分档阈值留空」同一处置，且与既有的 `QueueItem.outbound_qty = None`（数据缺失 ⇒ 4.2 整批降级）走同一条出口 —— 未配置 ⇒ 算不出出库量 ⇒ 退化为仅按 ABC 排序并标注降级。**这是本任务唯一的自主取值决定，登记在此**：待确认的是「N 是多少」，不是「要不要一个数」。④ 验证：`tests/logic/test_engine_package.py` 另加五条（并集逐项相等 + 无重名 + 逐名可达；六因子名的**内容**与**来源**各一条；四项可从包入口拿到且 `is` 同一对象；TODO 恰好四条；六因子内容写死在用例里而非与被测方共用一份）。⑤ 变异验证（`mutate12.py`）：M1 从 `__all__` 摘掉一个名字 ⇒ 红；M2 把 `WEIGHT_FACTORS` 抄成**值相等的字面副本** ⇒ **只有 `is` 那半红**、`==` 那半仍绿（两半各守一件事的实测证据）；M3 把 N 悄悄填成 `7` ⇒ 红。⑥ 另记一处**变异自身的坑**：M2 的第一版写成 `tuple(_MODEL_WEIGHT_FACTORS)`，跑出来是**绿的** —— 对**元组**调 `tuple()` 返回的是同一个对象（CPython 的实现细节），那个「副本」压根没复制，等于没变异；改用字面副本后才是真变异。与 10.x 的 `-qq` 同属「验证工具自身的陷阱」。**⑦ 2026-09-12 追加（N 的取值收口）**：业务方确认 **`N = 3`（按自然日）**，③ 里那条「本任务唯一的自主取值决定」由此关闭。按铁律**先改设计文档**：正本 `14` §3.2 的式子下补一段「N 的取值（2026-09-12 业务方确认）」（改前 `cp` 成 `14-推荐引擎与评分流程设计.md.20260912-074536.bak`，`diff` 核对**改动面恰为插入的那 6 行**、无旁及），`design.md` D14 表格第 1 行由「沿用 `14` §3.2 之外的默认（`08` 未给 N 的取值）」改为「**3 天**」并在表下补「欠账由四项减为三项」一段。代码侧 `DEFAULT_OUTBOUND_WINDOW_DAYS` 由 `int | None = None` 改为 `int = 3`，其注释块从「为什么不编一个天数」整段改写为「N 只在这里定义一次 + 本阶段不参与计算 + 降级与 N 无关」；包入口的 D14 表格同步改值，**第 1 行的 `TODO` 撤下**（欠账四条 → 三条），故 `test_the_d14_todo_block_lists_exactly_the_three_remaining_items` 的期望值随之改为 `["2","3","4/5"]`。**口径变了、行为没变**：阶段三没有取数来源，`outbound_qty` 仍由调用方给出（`app/api/routes/allocate.py` 恒传 `None`），N 要到阶段四的聚合落地时才真正参与计算；4.2 的整批降级表达的是「这份数据没拿到」，与 N 取多少无关 —— 故本次**零行为变更**，全量计数不变（784 / 逻辑层 493）。⑧ **改期望值时顺带补的一处守卫**（在本任务范围内，非新增范围）：`is` 那一半在**标量**上是结构性无效的（`None is None` / `3 is 3` 恒真，一份值相等的副本照样通过），故给 N 补一条「包入口只**绑定**它、不得再赋值一次」的正则断言。变异验证（`mutate12b.py`）：**N2 —— 包入口以相同值再定义一次，`==` 与 `is` 两半**都绿**、只有这条正则红**，正是它的存在理由；另三条 N1（3 → 7）红、N3（删一条 TODO）红、N4（把已确认的 N 挂回 TODO）红；四次变异后文件均逐字还原。⑨ **还留着一条同名不同物的 N**：`16` §360「ABC 统计窗口默认取样本全量 / 可按 N 天配置（**待业务方确认**）」是另一个 N（ABC 的派生统计窗口），本次确认**不覆盖它** —— 单列在此，勿与排序口径的 N 混为一谈
- [x] 1.3 建造数夹具 `backend/tests/logic/conftest.py`：内存 SQLite（复用 `tests/` 既有夹具）+ 一个 `make_scenario()`，可参数化生成 巷道/`AisleCap`/`Snapshot`/`InventoryItem`/`Material`/`JobOrder`/`WeightConfig`/`CapacityConfig`。验证：夹具自身有冒烟用例（空库能跑、四类输入全空时不抛异常）
- [x] 1.4 给分配器加**注入式时钟**（`now` 显式入参，引擎内不出现 `datetime.now()`）。验证：`tests/logic/` 用 grep 式静态断言「`app/engine/` 下不出现 `datetime.now(` / `random.` / `uuid`」，并有一条用例传两个不同的 `now` 断言释放判定翻转（`design.md` D2）。**实施补记（2026-09-12）**：① 代码侧的注入**早已就位** —— `allocate_batch(*, …, now: datetime)` 的 `now` 在第 6 组落主循环时就已是显式入参（docstring 里写着「整批只取一次」，`design.md` D2 第 3 条），`load_weights` / `load_aisle_state` 亦然；`ReleaseAt` / `now` 的口径（现场墙上时间、不带时区）由 `app/core/clock.py::require_wall_clock` 在入口处把关。故本任务落的不是新管道，而是它的**守卫**，分两半：② 静态面 —— `tests/logic/test_engine_package.py::test_no_engine_module_reaches_for_a_clock_random_or_uuid`：对 `app/engine/` 下的 `.py` 扫「`datetime.now(` / `datetime.utcnow(` / `random.` / `uuid`」四个串，**先断言扫到的文件数等于模块数 + 1**（路径写错时那几条搜索会**静默全过**，这正是 grep 式静态断言最常见的假绿）。扫之前用 `tokenize` 丢掉 `COMMENT` / `STRING` 两类记号：`scoring.py` 的 docstring 里就写着「引擎内部不调 `datetime.now()`」，**裸文本搜索会因为这句声明本身而失败** —— 丢掉散文后，禁令的解释才能自由写在注释与 docstring 里。③ 链路 —— `tests/logic/test_allocator.py::test_the_release_clock_is_the_injected_now_not_a_clock_read_inside`：同一批、同一张单，只换 `now`。09:00 时 B 类 70 板 > 该巷道 `cap_usable` 60（预留 20 未释放）⇒ 四级走尽、该单失败且给出「人工介入」；18:30 预留池已释放 ⇒ 落 `01`、`degraded is False`、`cap` 的 `note` 写出「可用 80 / 80 板」（因子读的是**本单落位前**的容量，故为 1.0 而非 70/80）。④ 变异验证（`mutate12.py`）：M5（在 `factors.aisle_of` 里塞一行 `datetime.datetime.now()`）⇒ 静态那条红；**M6（在 `allocate_batch` 里把钟冻结在 09:00）⇒ 静态那条照样绿、只有链路那条红** —— 这是「两半缺一不可」的实测论据：只断静态面的话，一个**收下 `now` 却从不用它**的分配器会全绿通过，而它恰恰是「同样输入必得同样输出」这条红线最现实的反例形态

## 2. 六因子与权重取数（F3）

- [x] 2.1 `factors.py` 落 `abc` / `cap` / `existing` / `batch` / `continuity` 五个因子的纯函数（取数与归一化到 `[0,1]`，各返回「值 + 取值说明」）。验证：`tests/logic/test_factors.py`，覆盖 `SC-002`（权重为 0 的因子贡献为 0）、`SC-003`（批次因子读**库存快照**既有批号分布，**不读 PO 批号**）、`SC-005`（既有同物料落位抬升同巷道得分）；含「库位号 `010104` → 巷道 `01` 按 `[:2]` 文本切片」的用例。**实施补记（2026-09-11）**：`SC-002`（权重为 0 的因子贡献为 0）的断言落在 3.3 的 `test_scoring.py` —— 它要的是权重，本文件只有单因子取值，放不进；`SC-003` / `SC-005` 已在本文件覆盖。批号口径见 `design.md` D16 的 `batch` 行（本单批号由系统在入库单建立时生成，既有批号集读快照，「不读 PO 批号」指的不是前者）
- [x] 2.2 `factors.py` 落 `station` 因子的取数与**降级判据**（`AisleStation` 无该巷道行 ⇒ 因子降级不参与评分）。验证：`tests/logic/` 断言首期预期状态（`AisleStation` 空表）下该因子不参与、且结果里出现 `factor_degraded["station"]` 及原因（`16` §394 / `A.4`）。**实施补记（2026-09-11）**：① 判据按铁律先改正本 —— `design.md` D16 原写「本阶段恒降级（表为空）」，那是**首期表空**这一事实的**推论**，不是判据；真正的判据是「该巷有没有 `AisleStation` 行」，且它是**方案级**的（一条候选巷缺行 ⇒ 该因子在全部候选上降级，否则各巷 `breakdown` 键集不相等，理由一落库就被 `schemas/reason.py` 的契约拒）。② 由此，「结果里出现 `factor_degraded["station"]` 及原因」这一半落在 **3.3**（`scoring.py` 才知道候选集、才谈得上聚合）与 **7.1**（组装 `payload_json`）；本任务落的是 `station_factor` + `load_station_weights` 与 `tests/logic/test_station.py`（首期空表全降级、有行取值、`[0,1]` 夹取、部分导出、按仓库过滤）
- [x] 2.3 权重取数：复用 `app/core/config_version.py` 的 `pick_current_version`，取 `WeightConfig` 当前生效版本的六列；**不存在生效版本则抛领域错误阻断**（不静默取默认）。验证：`tests/logic/test_scoring.py` 覆盖 doc 的「两版并存取新版且旧版仍可回滚」与「无生效版本 → 阻断、不产出方案」。**实施补记（2026-09-11）**：① 落点在 `scoring.py`（`load_weights`），返回 `{因子: 权重}`、键序按 `WEIGHT_FACTORS` —— 就是 `17` §10.1 的 `factors` 逐项，键序是 `design.md` 红线落地第 1 条要求的「遍历序显式给出」；② 阻断用 `BlockedMissingPrerequisite`（409），并**分类成因**：一版都没配 / 配了但均未到生效时间（两者操作员动作不同），`detail` 带 `warehouse_id`、`now`、版本数供端点渲染；③ 「两版并存取新版且**旧版仍可回滚**」里的回滚按 D1 的形态落成**补录一版编号更大的旧口径**（不是改回 v1），该用例同时钉住「编号最大者胜、而非生效时间最新者」，并自证改写已落库；④ 「不产出方案」这一半在引擎层是异常上抛的**推论**，端到端形态（整批阻断、无方案落库）归 9.x

## 3. 可行巷道集、预留池与综合评分（F3）

- [x] 3.1 `reserved.py` 落 `available_cap()`（A 类含预留；非 A 类仅 `cap_usable`；过了 `reserved_release_at` 释放给 B/C）与释放判定（现场墙上时间，`design.md` D7）。验证：`tests/logic/test_reserved.py` 覆盖 `AC-001`（近站台紧张 + A 类大量入队 ⇒ 慢流转品不占用预留池）、「超时后 B 类可用释放额度」、「`is_near_station` 为空且 `cap_reserved=0` 时收敛为全额可用」。**实施补记（2026-09-11）**：① **新发现一处时钟口径冲突，已按铁律先改设计文档**：D2 只注入一个 `now`，而释放判定要「现场墙上时间」（D7）、`pick_current_version` 比的 `effective_at` 在 `configuration.py` 里却写着「naive UTC」—— 同一个入参不可能同时是两种钟（本机 UTC+8 差 8 小时）。判据其实已由 D7 / D11 立下（**业务钟 = 现场墙上时间**：`reserved_release_at` / `effective_at`；**审计钟 = naive UTC**：`created_at` / `updated_at`，引擎不读），且 `config_version.py` 的报错文案本就教调用方传本地墙钟 ⇒ 记为 `design.md` **D17**，并订正 `configuration.py` 两处注释（注释级，无列/约束/迁移；全仓无写入方用 `utcnow()` 写 `effective_at`，**无行为变化**）。② 三处未在文档直给的口径各有用例钉住：`abc_class is None` ⇒ 按**非 A 类**（红线「非 A 类不得占用预留池」，未派生不构成「是 A 类」的证据；放行不可追溯，收紧只是可见的 5.2 告警）；`is_near_station` 为空**不特判**（靠三列自然收敛）；释放钟点**读配置行**而非写死 18:00（`16` §353~356 是可改口径）。③ 带时区的 `now` **不会被 `time` 比较拦住**（`datetime.time()` 丢 `tzinfo`，会静默差 8 小时），故由 `_require_wall_clock` 显式挡并给出 D17 的换法；`D11` 的批次号日期需要同一判据，届时提到一处共用
- [x] 3.2 `scoring.py` 落**可行巷道集**求解：**三判据** `cap 足够` / `巷道可用`（主数据存在性，D7）/ 非 A 类排除预留池（原第四判据「品质库区匹配」已从 `14` §3.4 步 1 移除，处置与逐文档改动面见 `design.md` D7）。候选巷道遍历序固定为 `aisle_no` 升序。验证：`tests/logic/test_scoring.py` 覆盖 `SC-004`（cap 已满的巷道不进入候选集）、「遍历序与 DB 返回序无关」（打乱插入序，断言输出序不变）。**实施补记（2026-09-11）**：① 落成 `AisleState`（`master` + `caps` 两半分开存）+ `load_aisle_state()` + `feasible_aisles()`；两半不预先取交集，是为了让「这条巷为什么不在候选集里」的三种成因（主数据没有它 / 这一版快照没有它 / 有它但容量不够）可分辨。② **超出任务书的一处入参**：`consumed`（本批已占格数）。任务书只列了三判据与遍历序，但 D4 的「进程内快照副本」要求同一批里前一张单的占用对后一张生效 —— 减在判据 1 里（而不是让分配器自己减），判据 1 才只有一个写法。③ **一处文档未给的口径，已先写进 `design.md` D4 的补记**：A 类占用的 30 格是「从整条巷道的可用量扣」还是「先吃光预留池再扣可用部分」，`14` §3.4 步 3 只有「扣减该巷道容量」一句。取**前者**（保守下界，两种读法下都不越红线；少给可见、多给不可追溯），并在用例里钉住「**不做**池子再分配」这件事 —— 池子怎么分属 4.x / 6.x 的记账侧。④ 遍历序用例**不经过 DB**：实测 `caps` 那条查询走唯一键索引、返回序**已经是升序**，故「靠 DB 返回序」在当前计划下看不出错，用例改为把键序打乱到与升序完全相反（前提断言 + 输出断言），免得它随一次建索引而退化成恒真式。⑤ 顺带把 `existing_factor` 里「占用格数 > 0」的判据提为公共的 `factors.require_order_cells`（两处都要它，文案与依据 `16` §171 只有一份）。全量 631 passed
- [x] 3.3 `scoring.py` 落综合评分：可用因子加权和 ÷ 可用因子权重之和，两位小数**十进制四舍五入**，比较用量化后值。验证：`tests/logic/` 覆盖 `SC-001`（近站台巷道得分最高并入选）、`SC-006`（全默认权重下与 `14` 的公式逐项一致）；含 `17` §10.1 的可追溯恒等式复算用例（**避开恰好落在半值上的测试数据**）。**实施补记（2026-09-11）**：① 落成 `score_aisles()`（加权和 ÷ 可用权重和、两位十进制半值进位、键序 `aisle_no` 升序、求和序固定按 `WEIGHT_FACTORS` —— 浮点加法不满足结合律，次序不同会让末位漂移，而末位漂移只在恰好半值时可见）+ `select_best()`（并列全列、升序；空集 `ValueError` 指向降级链，因为「没有巷可放」与「选出来是空的」在调用点长得一样）。② `SC-006` 用的是**文档自己的三个数**：分解取 `17` §10.1 算例的 `value`、权重取 `DEFAULT_WEIGHTS`、期望值取文档的 `scores`（0.79 / 0.58 / 0.43，`station` 降级 ⇒ 分母 0.80），三方对照；恒等式另有独立复算用例（用 `Decimal` 重算，两边都用 `float` 就成了同义反复）。③ 「比较用量化后值」的单测用 `round(0.575,2) == 0.57` 作前提断言（钉住算例确实落在半分位），期望 `0.58` —— 这条与文档「测试数据避开半值」不冲突：**期望值**避开半值，而**专门验量化方式**的这条必须落在半值上，否则它守的那一位没被测到。④ 2.2 补记里留在 3.3 的那半落地：`station_degrade_reason()`（方案级判据 ⇒ 一条候选巷缺行则该因子在全部候选上降级；**逐单求**，因为候选集随容量扣减而变）；整表空用 `17` §10.1 的措辞「巷道-站台主数据未导出」，部分导出则点名是哪条巷。⑤ 「每巷键集 = 六因子 − `factor_degraded`」在算分前**再校验一道**（另有 `schemas/reason.py` 的落库前那道）—— 只有契约那处时，键集错的表现是**分数算错**而不是报错。⑥ 六项权重全配 0（模型可表达）报 `ValueError`（写明是配置问题），不留 `ZeroDivisionError`。全量 643 passed

## 4. 队列优先级排序（F3）

- [x] 4.1 `priority.py` 落三项加法（未来 N 天出库量 + ABC 等级分 + 既有集中度增益），三项各自归一化后加权求和，系数取 D14 的默认常量。验证：`tests/logic/test_priority.py` 覆盖「同档内（多个 A 类）按出库量降序」、「A 类爆款先于 C 类慢流转品取得稀缺容量」。**实施补记（2026-09-11）**：① 落成 `abc_term()` / `outbound_term()` / `existing_gain_term()` / `score_priority()` + 常量元组 `DEFAULT_PRIORITY_WEIGHTS = (1/3, 1/3, 1/3)`（D14 的一处单点）。② **三项的归一化口径三处文档都没给**（`14` §3.2 只给式子、`17` §10.1 的示例值反推不出），故按 D16 的同一手法落成 `design.md` **D18**：`abc` 有证据（示例 `1.00` = A 类 ÷ 3，与 `abc` 因子同套，复用 `ABC_GRADE`/`ABC_GRADE_MAX`）；`outbound_qty` 与 `existing_gain` **明写「无示例可反推」**并给出取舍（分母取本批最大值 —— 唯一不需编造常数的分母；增益取二值 —— 唯一可归一化的读法，`1/(k+1)` 对 k 递减、与「收拢」反向）。③ **订正了事实来源的一处不自洽**（铁律：先改设计文档）：`17` §10.1 的 `priority.score` 原写 `1.42`，与 `14` §3.2 的式子不自洽（三项都是 `[0,1]`，权重合为 `1` 时不可能 > `1`；等权为 `0.70`，不加权也只有 `2.10`）⇒ 订正为 `0.70` 并补订正说明 + `priority` 行的值域；§10.7 响应示例的 `plans[].priority` 与 `plans[].aisles`（`["01","02"] → ["01"]`，**D16 单条裁决的遗漏传播** —— 该示例的 `job_order_id` 与 §10.1 的 `job_id` 同一条）同步订正。改前 `cp` 成 `17-数据模型设计.md.20260911-234300.bak`，`diff` 核对**改动面恰为那 4 行 + 2 处订正说明**；旁证：1.x 的 `test_reason_schema.py` 两处**早已**写 `0.70`，本订正让正本与已实现的契约一致、零测试改动。④ `score` **不做两位数量化**（与 `scores` 有意不同，理由写在模块 docstring）：量化会让「同档内按出库量降序」在归一化差 < 0.005 的两单上并档、落到 4.3 的 `job_order_id` 兜底，而那正是 `14` §3.2 要用出库量拆开的事。⑤ 系数按**权重和**归一化（业务方给的多半是 `(5,3,2)` 这类相对值）：纯缩放不改序，却把 `score` 的域钉在 `[0,1]`，与 `scoring.score_aisles` 同一手法；越界系数（项数不对 / 含负 / 和为零）当场 `ValueError`。⑥ 三条取数函数的**负数**一律当场报错：它们的失效形态都是静默的（负分量把单压到队尾、比值越界到契约层才拦得住）。⑦ 任务书「A 类爆款先于 C 类慢流转品**取得稀缺容量**」的**容量那一半**属分配器（6.x），本文件按 4.1 的范围断言**分的大小关系**（含一条对抗用例：C 类料已在库里、A 类是全新料，A 仍胜）；「谁先挑」的端到端断言落在 4.3 与 6.x。全量 661 passed
- [x] 4.2 `priority.py` 落**排序降级**：缺未来 N 天出库量 ⇒ 退化为仅按 ABC，并在理由中标注降级排序及原因（不中断分配）。验证：`tests/logic/` 断言该场景下 `priority.degraded = true` 且 `degrade_reason` 非空。**实施补记（2026-09-11）**：① 落成 `QueueItem`（frozen dataclass：单据号 + 两侧 ABC + `outbound_qty: int | None` + 既有巷道数 —— 只放本模块真正消费的事实，聚合仍在取数侧）与 `build_priorities(items) -> dict[str, PriorityPayload]`（键 = `job_order_id`）。`None`（这份数据没拿到）与 `0`（该料 N 天内没有交货单）**严格分开** —— 混成一个 `0` 会让降级永不触发，而不标降级的「仅按 ABC 序」在现场与「有数据但都很小」长得一模一样。② **降级只换一组系数、不另开算式**：`ABC_ONLY_WEIGHTS = (0, 1, 0)` 仍走 `score_priority`，故降级态下 `score` 恒等于 `terms.abc`（`17` §10.1 的「`score` 是三项的加权和」在降级态依然成立，一眼可复核）。集中度增益**一并**退出排序：`14` §6.2 退化的对象是**排序键**，且缺数据时现场站得住的判据只剩 ABC 档 —— 让散料凭「能并回既有巷道」插到 A 类之前，与「同档内不排序」直接冲突。③ **作用域 = 整批**（D18 已论证）：任一缺 ⇒ 全队列降级、每条都带同一个 `degrade_reason`（混刻度的队列序没有意义，与 `station` 因子「一条候选巷缺行 ⇒ 全候选一并降级」同源）。文案分「全缺（点名成品清单·历史流水未导入）」与「部分缺（`N/M` 条）」两种，并写明「分配照常进行，不中断」—— 操作员看见 `degraded` 的第一反应会是「这批是不是废了」。④ **`ABC` 未派生的处置是本任务补的口径**（`14` §6.2 只写了缺出库量）：任一队列项两侧皆空 ⇒ 该分量在**全部**队列项上计 `0.00`、系数取 `NO_ABC_WEIGHTS = (1/3, 0, 1/3)`（余两项按权重和重新归一化，`score_aisles` 丢弃降级因子的同一手法），并 `degraded=true` + 原因写明。只降出事的那几条同样是混刻度；代价是 ABC 这一项对本批无信息，而那正是「数据没到」的真相。**已登记 D18**（含未定项：是否要为它单列一类 `degrade_reason` 供前端分别渲染），**并向用户汇报** —— 任务书未要求这一形态，属实现期补的口径。⑤ `terms` 记**事实**、`score` 记**本批实际用的排序键**：部分缺失时两者故意不一致（有数据的仍写真实归一化值 —— 抹成 `0.00` 去凑自洽等式会掩盖「这份数据其实有」，比不一致更糟），差异由 `degrade_reason` 解释。⑥ **N 的常量不在本任务落**：D14 把 N 排在 `priority.py`，任务 1.2 又要求四个待确认默认值**集中**成具名常量；为免两处各写一份，降级文案**沿用 `14` §6.2 的原文术语**（「未来 N 天交货单出库量」），不在这里编天数 —— N 的唯一定义点与 `TODO` 随 1.2（D18 已记）。⑦ 重复单据号当场 `ValueError`：出参按单据号索引，静默覆盖会让**一张单从所有方案里消失**而队列看着完好（任务书未写，属防静默缺陷的最小守卫）。全量 671 passed（本任务 +10 条，含「一单缺 ⇒ 全批降级」「降级分恒等于 `terms.abc`」「ABC 未派生整批计零」「部分/全缺文案可分」「四项齐全不降级」「空队列」「重复单号」）
- [x] 4.3 `priority.py` 落**全序兜底**：`(priority 降序, job_order_id 升序)`，保证同分不同单的顺序确定。验证：`tests/logic/` 造三条同分队列项，连续两次分配断言队列序逐项相同（`design.md` D2）。**实施补记（2026-09-11）**：① 落成 `order_queue(items) -> list[RankedQueueItem]`（frozen dataclass：队列项 + 本批算出的 `PriorityPayload` 成对返回），排序键就是 `(-priority.score, job_order_id)` —— **这是「谁先挑稀缺容量」唯一的一处次序定义**，分配器（6.x）按它遍历、不再自己排一遍。② 成对返回而不是只回单据号：分配器两样都要（队列项告诉它这一单是什么，优先级既定次序、又是写进推荐理由的那份证据），分两次取会给「按一份算的序、拿另一份的分写理由」留出漂移余地，而 `score` 是个像模像样的浮点数，错配了没人会发现。分与序出自**同一次** `build_priorities` 调用。③ 单号按**文本**升序（不解析成数字：会在非数字单号上抛错或静默给出另一种序），已补进 D2 第 1 条；`priority` **不做两位量化**（D18），故比较的是加权和的浮点值。④ 用例里除任务书要求的「连续两次分配（**新建但相等**的队列项对象，模拟两次查询）」外，另加一条**入参排列无关性**断言（正序与逆序入参给出同一串序）—— 这是「全序」与「稳定排序」的分界：稳定排序会把 DB 行序保留成同分项的次序，而那正是一个未定义的遍历序（D2 要显式给出的就是它）。⑤ 4.2 的降级批同样走这条兜底（用例已覆盖：两条 A 类同分靠单号定序），「降级只换排序键、不放松顺序确定」。全量 678 passed（本任务 +7 条）

## 5. 降级链（F3）

- [x] 5.1 `degradation.py` 落**档位解析器**：返回有序的巷道分组列表；本阶段填档 0（近站台 `is_near_station IS TRUE`）与档 2（非近站台），档 1 / 档 3 为空集（`design.md` D5）。验证：`tests/logic/test_degradation.py` 断言四档结构存在、空档被跳过、首期只可达档 0 与档 2。**实施补记（2026-09-11）**：① 落成 `DEGRADATION_TIER_LABELS`（四档标签，顺序即优先级 —— `degrade_reason` 要逐字写「停在哪一级」，文案与档位必须同一来源）、`Tier(index, label, aisles)`、`TierPlan(tiers, unknown_near_station)` 与 `resolve_tiers(aisles, *, is_near_station)`（纯函数：不判容量、不算分、不选巷道 —— 那三件事各有一处口径）。② **四档恒为四个分组**（空档占位而非从列表里消失），故下探逻辑照常经过空档、不必为「首期只有两档」写第二条分支；用例断言「有成员的下标 = `[0, 2]`」。③ **`is_near_station = NULL` 进档 2，但必须与 `False` 分开可辨**（`app/models/master_data.py` 第 3 条：NULL = 权威值未到位，不得当 `False` 用）：未导出的巷**仍进档 2**（可达、不享近站台优先 —— 丢掉它等于一次静默的候选集收缩），同时**单列**在 `TierPlan.unknown_near_station` 里供 5.2 写进 `degrade_reason`。取数侧漏了这条巷（映射里没有它）走同一处置 —— 默认成 `False` 静默放过会把取数缺陷藏进一条看似正常的推荐。**已补进 D5**（含下游须知：首期整表未导出 ⇒ 每条 A 类单的首次可行档就是档 2，会命中「A 类降级告警」，故 5.2 的文案要把**成因**分开写，不能只报缺口数 —— 否则会让人去「移库腾挪」而真问题是数据没导出）。④ 档内按 `aisle_no` **文本**升序且与入参排列无关（D2 第 1 条）：档内次序影响「得分并列时选谁」（D16 的并列裁决），最终落进落库报文。⑤ 任务书说「档 2 = 非近站台」，实现上档 2 = `False` 或 `NULL` —— 这不是扩范围（NULL 无处可去：链是**固定四级**，不能新开一档），已按第 ③ 条处置并记明。全量 687 passed（本任务 +9 条）
- [x] 5.2 `degradation.py` 落降级原因措辞与 A 类告警文案：A 类爆款被迫降到远巷道 ⇒ 产出告警，文案含近站台缺口量，形如「近站台缺口 12 板，建议移库腾挪」。验证：`tests/logic/` 断言文案逐字含该句式与缺口数。**实施补记（2026-09-11）**：① 落成 `tier_stop_reason(plan, stop_tier)`（停档原因；停在档 0 ⇒ `None`）、`near_station_gap(order_cells, near_station_remaining)`、`near_station_alert_message(plan, order_cells, near_station_remaining)`、`is_alarming(abc_class, stop_tier)`。② **缺口量 X 取差额** `max(0, 本单需量 − 近站台剩余可用)`（不是整单需量）：主管据此决定是否移库腾挪，缺 2 板与缺 20 板处置不同，报整单会把这句指令变成「永远缺一整单」；spec 的示例（缺口 12 板）与差额读法自洽 —— 用例里就造了「需 20、剩 8 ⇒ 缺口 12」。③ **告警触发面 = A 类 ∩ 停在档 2 / 档 3**（`FAR_TIER_INDEX`，用下标不用标签比对：标签是给人看的文案，拿它当判据会在改文案时静默改掉告警面）；`abc_class = None` **不告警**（未知档不是 A 类）。参数化用例把两个方向都钉住（A+档 1 不告警、B/C/None + 档 2 不告警）。④ **文案加了「成因」句**（本次实现期加的，已记入 design 的 **D19**）：三种成因分开写（标志未导出 / 本快照无近站台巷道 / 容量不足）。理由：首期整表未导出 ⇒ **每条 A 类单的首次可行档就是档 2**（D5 的补记），这条告警会天天触发，只说缺口会把主管引去腾容量，而真问题是巷道主数据没导出。`「近站台缺口 X 板，建议移库腾挪」` 半句仍**逐字**照抄（spec 逐字要求）。⑤ 停档原因把「哪一档有候选但塞不下」（要腾容量）与「哪一档没有成员」（要补数据源）分开措辞 —— 由 `_tier_clause` 统一供 5.2/5.3 共用，未导出的标志另起一句点名巷道号。⑥ **接口裁定**：`resolve_tiers` 的入参是**候选集**（`master ∩ caps`）而非可行集 —— 传可行集会让两种空档含义分不开（已写进 `resolve_tiers` 的 docstring 与 D19）。全量 706 passed（本任务 +14 条，含 7 条参数化）
- [x] 5.3 落**四级走尽**的处理：该单标记失败、提示人工介入、**停留 `PENDING`**（不迁 `PLANNED`、不回写 `bulk_batch_no`），同批其余单照常出方案。验证：`tests/logic/` 覆盖 `AC-005`（降级链末端仍无容量 ⇒ 提示人工干预，不静默落位到非法巷道）。**实施补记（2026-09-11）**：① 落成 `AllocationFailure(job_order_id, message)` + `allocation_failure(job_order_id, plan)`：文案写全三件事（四档都走过 / 本单未产出方案且停留 `PENDING`、不在 `plans`、不回写 `bulk_batch_no` / 请人工介入），成因沿用 5.2 的同一措辞。② **失败单不带巷道字段，这是刻意的**：AC-005 的「不静默落位到**非法巷道**」在数据结构上就由「没有巷道字段」兑现 —— 给失败单编一条巷道等于产出一条「落在某条不存在的巷道」的记录；它因此也塞不进 `degraded_alerts`（`DegradedAlert.aisle` 必填 `min_length=1`，`17` §10.7）。用例直接断言字段集 = `{job_order_id, message}`。③ **失败 ≠ 降级**：链的末端没有「再往下」的档，把「哪儿都放不下」记成降级会让它在后验里与「降级到远巷道」同形。④ **任务一分为二的说明**：本任务在 `degradation.py` 落的是**判断与措辞**（D1 的模块切分：本模块不碰会话、不改状态）；「停留 `PENDING` / 不迁 `PLANNED` / 不回写批次号 / 同批其余单照常出方案」是**不在 `plans` 里**的自然结果，由 6.1 的主循环与 8.2 的状态迁移兑现；**端到端 AC-005 断言按任务书自身的安排在 10.2**（该行明写「落分配合规场景 AC-001/002/004/005」）。⑤ **未定项（需业务/前端口径确认，已记入 design 的 D19，并会向用户汇报）**：`17` §10.7 的响应**没有承载「分配失败」的字段**，故失败原因只在引擎结果里，操作员在端点响应里看不到「为什么这单没出方案」（只看到它不在 `plans`、状态仍 `PENDING`）。两种补法都动契约（加 `failures[]` / 复用 `degraded_alerts` 并放开 `aisle`）—— 本变更**不擅自改契约**，留给确认。全量 706 passed（本任务 +5 条）

## 6. 批量分配主循环（F4）

- [x] 6.1 `allocator.py` 落贪心主循环：逐单取可行集 → 选得分最高者 → **在内存快照上**扣减容量 → 下一条。验证：`tests/logic/test_allocator.py` 覆盖「选中得分最高的可行巷道」、「扣减对后续队列项生效」（剩 10 板，先占 10 再要 5 的单不再把该巷道计入可行集）。**实施补记（2026-09-12）**：① 落成 `to_occupied_cells()` + `AllocationItem`（转 `priority.QueueItem` 的 `queue_item()`）+ `AllocationOutcome`（结论与证据同体：`aisles` / `scores` / `breakdown` / `factor_degraded` / `stop_tier` / `degraded` / `degrade_reason` / `alert`）+ `BatchAllocation`（`outcomes` + `failures` + `alerts` 属性）+ `allocate_batch()` + `_allocate_one()` + `_build_breakdown()` + `_alert_for()`。整批只取一次数（D4）：入参是四个取数函数的产物，本函数不碰会话。② **逐档下探由本循环接线**（任务书只写了「取可行集 → 选最高分」，而 `resolve_tiers` 的分组必须有消费方，否则可行集为空时 `select_best` 抛 `ValueError` —— 即模块 docstring 的「四级链在主循环里兑现」）：`resolve_tiers` 收**候选集**（`master ∩ caps`），逐档取「本档成员 ∩ 可行集」，第一个非空的档即停档；`breakdown` / `scores` 的键集 = **停档的成员**（不是全部可行巷 —— 链的语义是先近后远，若全可行巷一起比总分，总分最高的可能是一条远巷道，链就白设了）；停档 ≥ 1 时由 `tier_stop_reason` 写原因。③ **`cap` 因子的分子取快照可用量、不减本批 `consumed`**，与 `feasible_aisles` 减 `consumed` 有意不对称（已登记 `design.md` D16）：`17` §10.1 的「`scores` 可由取值复算」是 7.1 的验证条件，而复算者手里只有快照与那一单、没有当时的 `consumed` —— 让因子值随竞争过程变化，理由就成了只有当事进程算得出的东西。一句话：**扣减管可行不可行，因子管好坏**。用例钉住「第二单的 `cap` 取值仍是 `可用 100 / 100 板`」与「其 `scores` 只含 `02`」并存。④ **A 类告警的生产者接上了**（5.2 的 `is_alarming` / `near_station_alert_message` 此前只有定义没有调用方）：`_alert_for` 在扣减**之前**取缺口（「这条单准备落位时近站台还剩多少」），告警的 `aisle` 取扣减落点而非并列全列（`17` §10.7 的告警只带一条巷，且要与 `plans[].aisles` 里的实际落点对得上）。**`near_station_remaining` 的口径是本任务补的**（`D19` 只给了差额公式、没给生产者）：= 本单候选里那些近站台巷道的当时余额之和，逐条按 0 保底再相加（负剩余会把缺口报大）—— 取候选集而非全仓近站台巷道，因为 `available_cap` 对 A / 非 A 给的数不同。已登记 `design.md` D19。⑤ **有效 ABC 单点化**：新增 `factors.effective_abc_class()`（单据侧优先、物料侧兜底，`16` A.4）并让 `abc_factor` 改用它、`AllocationItem.abc_class` 也用它 —— 预留池判据与 `abc` 因子读同一个值，两处各写一遍兜底顺序会让「物料 A 类、单据未派生」的单一边按 A 类取容量、一边按未知降级而不报错。⑥ **`existing_aisle_count` 由本文件从 `snapshot.profile(...).cross_aisle_count` 现取**（`AllocationItem` 不收这个字段）：两份来源分叉时「谁先挑」与「并回既有巷道」会按不同的既有巷数算，而两边都跑得下去。⑦ **`to_occupied_cells`（D6）此前无任务归属**，随本任务落在 `allocator.py`，`cartons_per_pallet` 先收进签名（本阶段不影响结果）：换算规则到齐时只改这一个函数体，而调用方已经把材料递进来了。⑧ **一处自我修正**：`test_the_highest_scoring_feasible_aisle_is_selected` 首版把第二条巷设成非近站台，结果它进不了档 0 的评分集，断言 `{'01'} == {'01','02'}` 失败 —— 是**用例的夹具错了**（「同档」是比较总分的前提），改夹具并写进 docstring，实现未动。全量 718 passed（本任务 +12 条）。**另**：全量首跑暴露一条**与本任务无关**的红 —— `tests/logic/test_token.py::test_issued_token_verifies` 漏传 `now=`，走真实墙钟，而 `ISSUED_AT` 固定为 2026-09-11 08:00（有效期 8 小时）⇒ 自 2026-09-11 16:00 起**必然**失败，且会让 pre-commit 的 L1 门禁挡下之后每一次提交。该文件 docstring 本就写着「纯函数、`now` 可注入」「到期即失效」，其余各例也都显式传了 `now`，故此例属漏写；补 `now=ISSUED_AT` 并写明理由（**一行、零行为变化**，属跨变更的顺手修复，向用户汇报）
- [x] 6.2 落**容量扣减不落库**：整批结束后断言 `AisleCap` / `Snapshot` / `Ledger` 三表**行数与内容均未变**；同一入参两次调用输出逐项相同（`design.md` D4 + D2）。验证：`tests/logic/` 的两次调用比对用例，逐项比 `aisles` / `scores` / `breakdown` / `priority` / 降级标记。**实施补记（2026-09-12）**：① **本任务无需改产品代码** —— 不落库由**结构**保证：`allocate_batch` 的签名里没有 `session`（入参是四个取数函数的产物），故它写不了库；已占格数只活在 `_allocate_one` 就地改写的 `consumed` 字典里，批一结束即丢（D4）。本任务的交付物因此就是这两条验证本身。② `_fingerprint(session, model)` 取**全部映射列**（不是挑几列 —— 漏一列就是给「悄悄改了这一列」留一个绿的口子），排序键用 `repr` 而非元组本身（元组逐位比较，遇到可空列上 `None` 与 `str` 同处一位会抛 `TypeError`，把一条断言变成一条用例错误）。③ **`Ledger` 先插一行真数据**：三张表里它是唯一会被别的流程写的那张（引擎自动写台账），空表的 `0 == 0` 是恒真式；连带要造 `Account`（`operator_id`）与 `executed_at`（都是 NOT NULL），且账号必须是真行 —— SQLite 开着 `foreign_keys=ON`，填占位整数会撞完整性错误而不是「随便造一行」。④ **先断言扣减确实发生了**（`PO-01` 占满 10 格的 `01` ⇒ 同批 `PO-02` 四级走尽失败），否则「表未变」对一个没跑起来的实现、或一个把每条单都塞进零格巷道的实现同样成立。⑤ 确定性用例**逐项比**而非整对象比（`aisles` / `scores` / `breakdown` / `priority` / `factor_degraded` / `stop_tier` / `degraded` / `degrade_reason` / `alert`，外加 `failures` / `alerts`），断言消息带 `job_order_id`：整对象比对失败只告诉你「不一样」，而漂移可能落在 `scores` 的小数末位、某条 `note` 或某个降级标记上，三处成因不同。第二遍**重跑四个 `load_*`**（故比的是新建对象，不是同一份），造的批里三种形态齐备（降级告警 / 四级走尽失败 / 正常方案）—— 只比一条顺路径，比不出降级与失败文案里的时钟、集合序有没有漂。⑥ **对断言本身做了变异验证**：用一个临时用例确认 `_fingerprint` 在写入一行后确实变化（跑完即删），否则「未变」的断言是空的。全量 720 passed（本任务 +2 条）
- [x] 6.3 落**批次号生成**：`BAT-<现场日期 YYYYMMDD>-<NN>`，`NN` 由当日 `bulk_batch_no` 前缀计数 + 1（无随机源）。验证：`tests/logic/` 断言格式、确定性（同输入同号）、跨日不串号（`design.md` D11）。**实施补记（2026-09-12）**：① 落成 `reasons.py::next_bulk_batch_no(taken, *, now)`（纯函数：认形状、按本日的最大序号进位、格式化成两位）与 `load_bulk_batch_nos(session, *, warehouse_id, now)`（取数：`DISTINCT` + 仓库过滤 + 日期前缀 `LIKE`）。**落在 `reasons.py` 是因为 D1 把 `17` §10.7 的响应侧字段划给了它**（`bulk_batch_no` 正是其中之一）—— 引擎的七个模块是既定的切分（1.2），不为一个编号新开第八个；它**不属于「理由」**，已在模块 docstring 里写明两段的分界。② **纯函数与取数拆开**：格式/进位/跨日用串就能验，不必建库；「哪些批次号算数」必须落到 SQL 验（仓库过滤 + 日期前缀过滤）。两半各有一类失效，且都**不报错** —— `bulk_batch_no` 没有唯一约束（D11 明写它不是业务主键，只是追溯用的分组标签），撞号不会有任何东西拦住。③ **序号取「本日已用最大值 + 1」，而非 D11 字面的「数量 + 1」**：本仓口径下两者同进（批次号只由 8.2 在同一事务写回，整批回滚不留行，故编号从 `01` 起稠密），取最大值是把同一条口径的**结果**照办、同时不把安全性押在「回滚必须干净」上 —— 一旦出现断档（`01` 与 `03` 并存），「数量 + 1」会生出 `03` 这个**已在用的号**。用例专门钉住断档形态（`01`/`03` ⇒ `04`）。④ **同批多条单只算一个号**：`bulk_batch_no` 是分组标签（一次请求最多 50 单共用，D10），按**行数**计数会让当日第二个批次变成 `51`；用例造 50 条同号单断言 `01 → 02`。⑤ 序号用 `\d+` 而非定长：位数过 99 要**认得出来**（`BAT-20260908-100` 是第 100 批），跳过会让下一天重来并与既有号相撞；用例覆盖三位数。⑥ **守卫提到一处共用**（3.1 的补记预告过，已登记 `design.md` D17）：新增 `app/core/clock.py::require_wall_clock(now, *, purpose)`，`reserved.py` 改为 import 它（行为不变，`purpose` 让报错指名是哪个判据在拒）；放 `app/core/` 而非 `engine/`，**引擎七个模块之间因此不多出一条依赖边**（若让 `reasons` 去 import `reserved` 只为借一个守卫，就把 D1 那张单向图读乱了）。`test_reserved.py` 那条 aware 断言的 docstring 随之订正（判据与匹配串未动）。⑦ 本条**只生成号，不写回**：写回 `bulk_batch_no` 与状态迁移、以及「计数与写回同事务」（D11）属 8.2。全量 731 passed（本任务 +11 条）。**另注**：`test_batch_no.py` 首版在一处 docstring 里写了 `\d{2,}`，触发 Python 的 `SyntaxWarning`（非 raw 字符串里的无效转义）—— 改为文字表述；这类警告在 `-p no:warnings` 下不会显形，是在 RED 那一跑里看到的

## 7. 推荐理由与预测跨巷道（F4）

- [x] 7.1 `reasons.py` 落 `payload_json` 组装（`17` §10.1）：每候选巷道 × 每参与因子的取值与**取值说明**、六项权重、`scores`、`priority`（含降级标记）、`factor_degraded`、方案级 `degraded` / `degrade_reason`。验证：`tests/logic/test_reasons.py` 断言三条不变量 —— 降级因子不出现在分解里、`scores` 可由取值复算、列与 JSON 的同名字段一致。**实施补记（2026-09-12）**：① 落成 `build_reason_payload(outcome, *, weights, predicted_cross_aisle) -> ReasonPayload`（`reasons.py` 的第二段，紧接 6.3 的批次号）。② **调用点定在端点，不在分配器**：D1 的图写着「`allocator` **向下**调 `reasons`」，而理由体要的 `predicted_cross_aisle` 是**整批回溯**的产物（`14` §3.4 步 5），单条 `AllocationOutcome` 看不见同批的其它单 —— 分配器若自己调本模块，就得在循环里拿到一个循环结束后才存在的数。故落成的调用序是 `allocate_batch` → `predict_cross_aisles` → **端点**逐单组装，依赖边为 `reasons → allocator`（**反向没有**：分配器不认识 `reasons`）。**已按 D17 那处补记的样式登记进 `design.md` D9**；与 D17 的区别是那条为纯粹的实现位移（时钟提到 `app/core/clock.py`，语义未变），这条是**依赖方向的更正**（理由体渲染的就是分配器的决定）。引擎仍是六个模块（1.2），不为这条边新开第八个。③ **六项权重整份搬进报文**（`factors`）：必须是**打分时用的那一份**（`load_weights` 的产物，整批同一份）。重新读一次库会让「按这一份算的分、拿那一份写的理由」成立 —— 而 §10.1 的不变式要求复算的人**只用这份 JSON**（他手里没有当时的权重行）。用例断言 `payload.factors == DEFAULT_WEIGHTS`。**降级因子的权重也在里面**（契约给全，参与与否由 `factor_degraded` 表达 —— §10.1 的算例里 `station` 降级后 `factors` 仍有它的 `0.20`，分母 `0.80` 就是刨掉它的结果）。④ **`degraded` / `degrade_reason` 原样取自 `outcome`，不在组装里二次推导**（不从 `stop_tier` 或 `factor_degraded` 反推）：方案级降级只有 `degradation.tier_stop_reason` 一个来源，重推会给「同一件事两种算法」留出余地。8.2 写 `RecommendationPlan` 两列时从本结果取值 ⇒ D9 第 3 条的「同一次写入的投影」在**代码**上成立、而不只是一句约定。⑤ **不变量 2 的复算用例刻意造了 `station` 降级的算例**（分母 `0.80` 而非 `1.0`）：六因子齐全时分母恰好是 `1.0`，归一化那一步写没写都看不出差别 —— 而它正是「有因子降级时满分仍为 1.0、跨轮次可比」的落点。复算两侧都用 `Decimal(str(v))`（**分子分母都取报文自己给的数**），故「分母没重新归一化 / 取值来自别处 / 量化方式不同」三种偏差都能变红；半值进位规则本身已由 `test_scoring.py::test_scores_are_decimal_half_up_not_python_round` 钉住，这里不重复钉。⑥ **第三条不变量在 7.1 只能验一半**（列的写入属 8.2）：引擎侧验到的是「两列的同源事实只有一处」+「序列化后同名字段保持同一形态」，列本身的写入断言在 8.2 —— 已写进用例 docstring，免得后人以为这里漏验了。⑦ **`_item` 的 `batch_no` 缺省值是个陷阱**（本任务踩到）：`AllocationItem.order_batch_no = None` 会让 `batch` 因子**降级**（「本单无批号……无从比较，非「无同批」」），于是「六因子齐全」那条断言当场变红；三处用例改为显式给批号并在注释里写明原因。要验「只有 `station` 降级」的形态时尤其要给全 —— 多降一个因子，就没有哪条断言能说清是哪一种降级（7.4 的算例同理）。全量 741 passed（本任务 +3 条）。**订正（2026-09-12，8.1 落地时）**：本条 ④ 末句与 ⑥ 里的「列的写入属 **8.2**」**应读作属 8.1** —— 写 `RecommendationPlan`（含 `degraded` / `degrade_reason` 两列）是**端点的落库步骤**，8.2 只做状态迁移与 `bulk_batch_no` 回写。`design.md` D9 的对应措辞已订正；两处写错的同一个任务号会把人指向一个不看这里的地方
- [x] 7.2 落**预测跨巷道**回溯计算（`14` §3.4 步 5）：`| 既有快照中该物料占用的巷道 ∪ 本次分配的巷道 |`，阈值取 `CapacityConfig.same_material_cross_aisle_threshold`。验证：`tests/logic/` 覆盖 `AC-004`（既有 2 巷道 + 新分 1 巷道 ⇒ 3，未超阈）；含「库位号按 `[:2]` 切片、不数值化」的用例。**实施补记（2026-09-12）**：① 落成 `allocator.predict_cross_aisles(batch, *, snapshot, threshold) -> dict[str, PredictedCrossAisle]` —— **收整批、必须在 `allocate_batch` 跑完之后调**（回溯的时点是定义的一部分，见 7.1 的 ②）；**键 = 出了方案的料号**（四级走尽的单没有方案，预测也就无处可挂；多一个键会让调用方以为那张单有方案）。② **既有集复用 `SnapshotIndex.profile().plates_by_aisle` 的键**，不另发一条 `substr` 查询：那几个键与三个库存因子同过 `factors.aisle_of`（`location_code[:2]`），口径因此全仓只有一处 —— 另写一条会让「因子按 `01` 聚、预测按别的聚」静默成立，而两边都在报一个看起来正常的整数。行集与切片与 D9 那句「按 `substr(location_code, 1, 2)` 去重」完全相同（同源同表），已记入 D9 的补记。③ **并集只取 `len()`**（D2 第 1 条：`set` **仅**用于去重）：按集合的遍历序取任何值，都会让同一份输入在两次运行里给出不同的数。④ **阈值是入参**，由端点从 `CapacityConfig.same_material_cross_aisle_threshold` 取（`16` §353~356 的默认值表：它是**可改的口径**，不是常量），不在引擎里回落到 `Settings` 的引导值。用例把阈值调成 `2` 断言 `exceeded` 翻转：写死 5 的表现是「现场调小之后不再触发，而没有任何地方会报错」——于是「预知集中度」只报好消息。⑤ **`AC-004` 照任务书的预演形态**：既有 2 巷（`01`/`02` 各 1 格、塞不下本单 ⇒ 不可行）+ 新分 1 巷（`07`，100 格）⇒ `3 ≤ 5`；用例先断言落点确是 `07`（前提：新分的巷**不在**既有集里，否则这条用例测不到并集）。落位后实测属后验（F8），此处算的是「若照此落位会变成几」——口径相同、时点不同。⑥ **`[:2]` 的用例**：库位号 `010104` 落在 `01`、本单也落 `01` ⇒ 报 `1`。三种错法都会给 `2`（`[:3]` = `"010"`、`int("010104")` = `10104`、取整 `1` 与巷道号 `"01"` 不相等），而 `1` 与 `2` 都长得像正常的数 —— 前导 0 一丢，多出来的是**不存在的巷道**。⑦ **整批并集的用例**：同料号两张单分到 `01` / `02` ⇒ **两张都报 2**（逐单算会各报 1，而它看起来同样「正常」：「本次分配给该物料的巷道」这句话的主体是料号，不是单据）。⑧ **键集的用例直接断言映射的键**（不经 `payloads` 取数 —— 那样看不出多出来的键）：造「一条塞不下的单 + 一条正常单」，断言 `set(predictions) == {"MOK"}` 且失败单只在 `failures` 里。全量 741 passed（本任务 +5 条）
- [x] 7.3 落**同批跨巷道不预测**：本契约本期只给同物料口径，理由中不含同批预测（`14` §3.1 / §3.4 步 5 —— 批号在分配时刻**已知**，不给同批口径是**本期范围**而非时点所限；2026-09-11 更正）。验证：`tests/logic/` 断言 `payload_json` 不含同批跨巷道键。**实施补记（2026-09-12）**：① **本任务无需改产品代码**，「不给同批口径」由**结构**兑现：`ReasonPayload` 的 `extra="forbid"` + 契约校验器（`_contract_invariants`）使字段集封闭，`predict_cross_aisles` 也只产出同物料的那一个数。本任务的交付物因此就是验证本身。② 用例的做法**不是**「查某个键在不在」而是**断言集合封闭**：`set(model_dump(mode="json")) == ` 十个字段（`REASON_FIELDS`）、且 `predicted_cross_aisle` 的键集恰好 `{material, threshold, exceeded}` —— 只查一个键在不在，测不出「换了个名字塞进来」。③ **关键是让两个口径给出不同的数，否则用例是空的**：造同批（`GJP1`）两张单分到 `01` / `02`（⇒ 同批口径是 `2`），而该料号在 `03` 还有既有库存（⇒ 同物料口径是 `3`）；断言报文里报的是 **`3`**。不造这个形态，用例对「实现报的其实是同批数」同样是绿的。④ 「本期范围而非时点所限」这句已登记 `design.md` D9（2026-09-11 的更正），本任务不重复登记。全量 741 passed（本任务 +1 条）
- [x] 7.4 落**两种降级分开承载**：列 `degraded` / `degrade_reason` 只描述降级链；`factor_degraded` 只描述缺失因子，内容不得互换（`design.md` D8）。验证：`tests/logic/` 造一个「同时容量不足与数据缺失」的方案，断言两个字段各自的内容与不交叉。**实施补记（2026-09-12）**：① 造「两件事**同时**发生」的算例：近站台 `01` 只有 5 格、塞不下 10 板的 A 类单 ⇒ 走降级链停到远巷道 `07`；两条巷都**不给** `station_weight`（该巷没有 `AisleStation` 行）⇒ `station` 因子级降级。② 三条断言各守一处失效：**方案级**只说容量/档位（含「停在「远巷道」档」「近站台无可行容量」，且**不含**「未导出」/「主数据」—— 混进去会让操作员去补数据，而真问题是近站台塞不下）；**因子级**只说数据缺失（含「未导出」，且**不含**「容量」/「档」）；两句原因**互不为对方的内容**（不相等、互不包含 —— 「内容不得互换」的最短形态）。③ 再验**序列化**后两个字段仍各就各位（落库的就是这一份），且方案级降级带得住原因 —— 对应 `RecommendationPlan` 的 `CHECK(degrade_reason_required)`（「降级不静默」的库层约束）。④ **`station` 降级的判据是「该巷没有 `AisleStation` 行」，不是「权重为 0」**：首版给了 `station_weight`，跑出来 `factor_degraded` 是**空集** —— 这一处只有真跑过才知道，记在此处备查（「缺数据」与「数据取值为 0」是两件事，`FactorOutcome` 的两态也正为此分开）。全量 741 passed（本任务 +1 条）

## 8. 端点 `POST /api/allocate/batch`（F4）

- [x] 8.1 按 TDD **先写端点测试**（RED），再在 `backend/app/api/routes/allocate.py` 实现，并在 `backend/app/main.py` 注册路由。验证：`tests/api/test_allocate.py` 覆盖「显式集合驱动分配」（5 条 PENDING 只提交 2 条 ⇒ 其余 3 条状态与方案均不变）、响应 `plans` 按 `priority` 降序。**实施补记（2026-09-12）**：① 落成 `app/api/routes/allocate.py`（原为 12 行 router 桩），`main.py` **无需改动** —— `allocate.router` 早已在 `ROUTE_MODULES` 里（测试里的 200 即证）。端点是**组装点、自己不打分**：`_requested_ids` → `_load_orders` → `_current_snapshot` → 容量口径/权重/快照索引/巷道状态/站台权重/近站台三态/物料 → `next_bulk_batch_no` → `allocate_batch` → `predict_cross_aisles` → 逐单 `build_reason_payload` + 落行 → **一次** `commit()`（事务边界属端点；`deps.get_db` 只负责回滚）。② **四条拒绝路径全部落在进引擎之前**（D10 只写「拒绝」，没点类别）：未知 id / 同批重号 / 非规范形式 ⇒ `ValidationBlocked`（422）；声明的 `snapshot_version` 与实际不符 ⇒ `StateConflict`（409）。**为什么必须在外面拦**：放进引擎会撞 `build_priorities` 的裸 `ValueError` ⇒ 以 500 泄出，一个用户输错的 id 不该表现成服务端故障。四条路径都验了**零方案行落库**。③ **线上标识 = `str(JobOrder.id)` + `^[1-9]\d*$`**：队列、重号检查、引擎内的扣减索引**三处都按字符串做键**，放 `"007"` 进来会让同一条单以两个身份入队、被两次扣减，而**全程无一处报错** —— 产出一份看起来正常的方案，只是那条单算了两次。连带后果已登记（`design.md` D10 的 ①）：D2 的 tiebreak 实际落在**十进制串的字典序**（`"10" < "9"`），仍确定，但别按数值大小预期。④ **快照**：取该仓库 `version_no` 最大者；**无 `Snapshot` 行 ⇒ 端点 409 阻断**。与 9.5 是**两层各管一半**：引擎（喂 `SnapshotIndex.absent`）照常分配、三个库存因子降级**不阻断**（9.5 在 `tests/logic/` 验的就是这一半）；端点在进引擎前闸住，因为 `snapshot_version` 是必填且带 pattern 的出参（唯一诚实来源是 `snapshot_time`，而 D10 明写不许拿 `version_no` 顶），且 `AisleCap` 按快照存 —— 无快照时每条单都会走完四级降级链，把「数据没准备好」记成 N 条「分配不出来」的方案并全部落库。两条理由与它与 spec 143 行 / 9.5 字面的关系已登记（`design.md` D10 的 ②）。⑤ **`outbound_qty` 阶段三恒为 `None`**（不是 `0`）：本期无出库数据，`0` 会被读成「有出库量且为零」——一个本不存在的观测。`CapacityConfig` 无生效行时退回 `Settings` 引导值（`18:00` / `5`），**退回逻辑放在组装点**：引擎刻意不认识 `Settings`，拉进 `engine/` 会让「引擎只认传入的配置」这条边失效。两处同样登记在 D10 的 ④⑤。⑥ **本条只落「出方案」这一半**：状态迁移与 `bulk_batch_no` 回写属 8.2 ⇒ 用例**刻意不断言**已分配单的 `status`（那会把 8.2 的行为提前钉死在本用例里）；不写台账/cap 属 8.3、权限属 8.4、规模上限与空数组属 8.5，各归本主。⑦ 用例 15 条（`tests/api/test_allocate.py`）：本条的验证条件两条（显式集合驱动 —— 5 条 PENDING 只提交 2 条 ⇒ 其余 3 条状态与方案均不变；`plans` 按 `priority` 降序）＋近站台先占而后推外溢、**非 A 类被推外溢时不出告警**（A 类 ∩ `stop_tier.index >= 2` 才告警）、plan 行 = 写进去的报文且两列是 JSON 的**投影**（D9 第 3 条因此在**代码**上成立，7.1 的 ⑥ 只验得到引擎侧那一半）、跨巷道阈值取自容量配置行（现场把阈值调小 ⇒ `exceeded` 翻转）、容量配置缺席退回引导值、快照版本回显与不符即 409、以及 ② 的四条负例。⑧ **首跑即全绿**（15/15），故改做**变异验证**而不是反复调断言：分别改「阈值的来源」「`outcomes` 的顺序」「落行的顺序」⇒ 恰好 3 条变红，改回后 grep 确认只剩预期的回落分支 —— 绿得顺手时更要钉住断言真的咬得住。⑨ **两个夹具陷阱**（都踩过）：`seed()` **必须 `commit()`** —— 端点走的是**另一个会话**（`app.state.session_factory`），不 commit 就什么都看不见，而表现是一片 404/422、不像「少了 commit」这种好定位的错；`Account.password_hash` 给占位串、令牌直接 `create_session_token(...)` 签，**绕开 bcrypt**（本机 passlib 1.7.4 + bcrypt 5.x 调用即抛，见 `CLAUDE.md` §五）。⑩ 全量 **756 passed，耗时 5.19s** —— 首次越过 pre-commit 注释里的 `<5s` 目标线（前一任务是 741 条 / 4.26s，本任务 +15 条 / +0.93s）。hook **没有超时**，故它是**目标**而非硬失败；逐条实测夹具建表 0.02~0.04s、调用 0.02~0.04s。已就此单独报告，不擅自放松口径
- [x] 8.2 落状态迁移与回写：成功者 `bulk_batch_no` 非空且与响应一致、状态 `PENDING → PLANNED`；其余状态**整体拒绝**（不部分成功）、不新增未定义迁移。验证：`tests/api/` 覆盖「状态迁移与批次号回写」、「非待分配状态被拒绝」；含重复提交同一批（第二次必被拒）的用例 —— 即乐观锁语义经状态守卫兑现。**实施补记（2026-09-12）**：① 三处落成：`_require_pending(orders)`（整批预检，紧接 `_load_orders`）、回写写在**「逐单组装理由并落行」的同一个循环里**、模块 docstring 那节「8.1 的范围：不迁状态、不回写批次号」改写成 8.2 的落点（它的每句话在落地后都会变成假话，留着比删掉更险）。`main.py` 仍无需改动。② **非 `PENDING` ⇒ 整批 409 `state_conflict`、零写入**。「整体拒绝」有两种实现位置：写在写入循环里逐单判，或整批预检 —— **取预检**。逐单判的形态是「前几张写、后几张拒」：`StateConflict` 抛在循环中间时前面几张已经 `session.add` 了，虽然整批回滚仍然是零写入，但那把「整批拒绝」**押在「调用方一定不 commit 中间态」上**；预检不押这个。放在引擎之前也是同一原则（整批拒绝不该依赖「引擎恰好没出方案」这个巧合），与 8.1 的报文级拒绝同一条。报**全部**越界单（按 id 数值序）而不是第一张：调用方要一次把集合改对，一次报一张会让他改一轮再撞一轮。三个参数化形态各有现实来路：`PLANNED` = 同一批提交了两次、`REJECTED` = 驳回后**忘了重新入队**（它要回 `PENDING` 才能再进队列，故这里拒得对）、`CANCELLED` = 已移出本次批量（终态）。③ **回写的判据 = 「这一单在 `batch.outcomes` 里」**，不是「被提交了」：四级走尽、只出现在 `batch.failures` 里的单**停留 `PENDING`、不领批次号**（D12：「该单停留 `PENDING`、提示人工介入」；`PENDING → PENDING` 是 `15` §3.1 那条合法自环）。失败单若也写上批次号，「这单属于 `BAT-xxx` 批」在追溯时就是假话，且它同时不再是「可重试的待分配单」。实现上这不是额外的一个判断 —— 回写就在落行那个循环里，**与落行同一条判据** ⇒ 「在 `plans` 里」与「被回写了」不会分叉（这条也写进了端点 docstring 的末尾）。④ **迁移走共用状态机、版本跟着走**：`order.status = assert_transition(order.status, JobStatus.PLANNED)` + `bump_lock_version(order, expected=order.lock_version)`。这里本可以只写 `order.status = JobStatus.PLANNED`（更短，且今天完全等价）—— 不那样写是因为：那条 `assert_transition` 守的是**预检被放宽**的那一天（改成「跳过非 PENDING」时，非法迁移会在写入处当场报错，而不是静悄悄写出一行 `CANCELLED → PLANNED`），「不新增未定义迁移」这条要求才不只在 `state_machine` 里成立。版本推进同理要点破：**请求里没有版本号**（D10 的请求体只有三项），故「重复提交第二次必被拒」是**状态守卫**兑现的、不是版本比对（任务书原文即如此）；推进 `lock_version` 保的是**下一个**带着版本来的调用方（二次确认卡）—— 行变了而版本没变，那边会以为自己读到的还是旧内容，而那正是 D1 要防的静默覆盖。故 `bump_lock_version` 里那个必然通过的比对是「增量语义只有一处定义」的代价，不是一次校验（已在代码注释与 `design.md` 里写明，免得被读成「这里在校验什么」）。⑤ **409 的 `detail` 形状定死**：`{"not_pending": [{"job_order_id", "status"}], "expected": "PENDING"}`，按 id 数值序。前端要判「这一批里哪几张要剔除」，只给一句自然语言就只剩拆字符串一条路；顺序不定则同一批两次调用给出两份顺序不同的明细。⑥ **测试加在同一个文件、另起一节**（`test_allocate.py` 的「8.2 状态迁移与批次号回写」，4 条用例 / 6 个参数化形态：回写与响应一致 + `lock_version` 推进 + 未提交单三件不动；三个非 `PENDING` 状态各自整批拒绝且零写入；失败单不被回写且不占容量；重复提交第二次被拒且不新增方案行）。8.1 那节的 docstring 本写着「8.2 不在本文件」—— 落地时**没有删那句，而是分节并说明为什么分节**：8.1 的用例原样不动（它们仍不断言 `status`），8.2 的断言集中在一节里，「哪条用例守的是哪个任务书」因此还读得出来；混在一起之后，一行 `assert order.status is ...` 到底属哪个任务书就只能靠读 diff 猜。另开文件则要跨文件导夹具，不值。造数侧给 `_capacity_scenario` 加了 `specs=` 参数（要造「已排过计划的单」「塞不下的单」这类个别形状时逐个写 spec 更直白，顺序也有意义：同优先级下先给的先挑巷道），并加 `_order_spec()` 收公共形状。⑦ 首跑全绿（8.1 的 15 条也照旧全绿）⇒ 与 8.1 同样改做**变异验证**：去掉 `_require_pending` 调用 ⇒ 4 例红；不推进 `lock_version` ⇒ 1 例红；回写别的批次号 ⇒ 3 例红；迁到 `PENDING` 自环 ⇒ 3 例红 —— 每处都恰好打在该管的用例上；改回后 `diff` 确认与验证过的版本逐字相同。⑧ 全量 **762 passed，5.41s**（前一任务 756 / 5.19s，本任务 +6 条 / +0.22s）。`<5s` 这条目标线**连续两个任务被越过且仍在涨**，已再次上报
- [x] 8.3 落「不写台账」与「不写 cap」：一次成功的批量分配后断言 `Ledger` 与 `AisleCap` 均无新增行，作业单停在 `PLANNED`。验证：`tests/api/` 的断言用例（红线「未确认不产生台账」）。**实施补记（2026-09-12）**：① **本任务无需改产品代码**（与 7.3 同类）：「不写台账、不写 cap」由**结构**兑现 —— 端点的写入面只有 `RecommendationPlan` 三处（D4：扣减只落内存），8.2 的回写只碰 `JobOrder` 的两列加版本。本任务的交付物就是**验证本身**。② **断言按「逐列取值」而不是「行数」**：`_cap_rows()` 取每条巷道的 `(id, cap_total, cap_reserved, cap_usable, is_near_station, updated_at)` 整份快照比对。行数比对对**原地改掉 `cap_usable`** 是瞎的，而那恰是这条决策最现实的失效形态（阶段四接 cap 增量时顺手就地减一笔，行数不变、冻结值失真）。用得上 `updated_at` 只是因为模型恰好带了这一列（`17` §3.4 为漂移校正留的，带 `onupdate=utcnow`）—— 行被 UPDATE 过（哪怕改回原值）时间戳就会动，于是它是「这一行有没有被碰过」的证据。③ **反向也要钉**：同一用例先断言「两条方案行在、状态停在 `PLANNED`」。只验「没写」的话，一个空转即返回 200 的端点同样能过 —— 这条红线的反面不是「多写了」，而是「什么都没发生却报成功」。④ `CapAlert` 不在断言内：它另一个种类（台账事务增量失败）属阶段四，本端点没有任何路径能写出它（用例 docstring 写明，免得后人以为漏验）。⑤ **首跑即绿**（行为本就成立）⇒ 照 7.3 / 8.2 的处置做**变异验证**：注入「写一行 `Ledger`」与「原地 `update aisle_caps set cap_usable = cap_usable - 10`」两处变异，各自**恰好**让这条用例变红，改回后与验证过的版本逐字相同。⑥ **一处流程事故（已修复，登记备查）**：变异脚本用 Python 的 `pathlib.write_text()` 还原文件，Windows 下 `newline=None` 会把 `\n` 译成 `os.linesep` ⇒ 整份 `allocate.py` 一度被写成 CRLF。`diff` 当场报「1,502c1,502」整文件差异，`cp` 还原 + `grep -c $'\r'` 确认清零。**这类事故不报错**：CRLF 的 Python 文件照样 import、照样跑绿，只会以「每次改动都整文件变红」的形式在 `git diff` 里出现 —— 下次用 `write_bytes` 或 `newline=""`。全量 **763 passed / 5.41~5.88s**（本任务 +1 条；耗时在两跑之间摆动 0.47s，比本任务新增的那一条用例大一个量级 ⇒ 墙钟读数含机器负载，不足为据）
- [x] 8.4 落认证与权限口径：未携带凭据 ⇒ 401（由既有中间件覆盖）；**不施加 403**（`permission` 规格的「v1 不做端点级资源鉴权」）；`engine.invoke` 恒不可授予（`AUTO_ONLY`）。验证：`tests/api/` 覆盖 401 用例；另有一条对照用例断言 `ROLE_PERMISSIONS` 中 `inbound.operate` 的持有者为 `warehouse_keeper` / `admin`，`planner` / `supervisor` 为否（`design.md` D10）。**实施补记（2026-09-12）**：① **本任务同样不改产品代码**（与 8.3 同类）：401 早已由 `AuthMiddleware` 对 `/api/*` 全局返回（13 §6.1 第 1 层、`middleware.py` 的模块 docstring），端点上没有一行是 8.4 加的。交付物是**验证本身**。② **401 用例测四个形态而不是一个**：缺 `Authorization` 头、只有方案名没有凭据、凭据不是 JWT、签名被改过 —— 它们走中间件里**四条不同的分支**（`_extract_token` 的两个早退、`decode_session_token` 的解析失败、签名校验失败），收敛到同一个 `unauthenticated`。一条不够：少测哪条，那条就可能被顺手改成放行而没人发现，而这四条都是「不认证也能进来」的入口。③ **断言的是「端点整个没跑」，不只是状态码**：把鉴权写在端点内部（先取数、再检查）的实现**同样回 401**，但库里的方案行与作业单已经动过了。故除状态码与 `error` 外，还断言 `plan_rows() == ()`、全部作业单仍 `PENDING`、`bulk_batch_no is None`（8.2 的两处回写都没发生）。变异验证：让 `_requires_auth` 恒 `False`（等于摘掉中间件）⇒ 该例红。④ **「不施加 403」单独立了一条用例**（任务书的要求文字里有这一条，规格是它的出处）：持 `planner` 凭据调本端点、断言 **200**。它拦的是**提前收紧** —— 现在就挂 `PermissionChecker`，会让 13 号此刻真实的调用方当场收到一个 403，而那个 403 是实现自己发明的拒绝。用例 docstring 写明：**M5 落地时这条应当变红，那是它该有的样子**，届时改成断言 403 并移出成功路径，**不要删掉它**（删了之后「本端点的鉴权口径是哪一版」就再没有可执行的说法）。变异验证：在端点里插一句 403 ⇒ 该例红。⑤ **对照用例的断言走 `check()` 而不是直接查 `ROLE_PERMISSIONS` 字典** —— 与任务书原文（「断言 `ROLE_PERMISSIONS` 中 `inbound.operate` 的持有者…」）的字面形式**有意不同，登记在此**：字典只是数据，真正的契约是 `check()`（将来 checker 要调的就是它，且它把 `AUTO_ONLY` 的短路也算在内），断言字典会在「有人把这两个权限误加进某角色集合」时给出与运行期不一致的结论。四个角色（`warehouse_keeper` ✅ / `planner` ❌ / `supervisor` ❌ / `admin` ✅）逐一参数化，任务书点名的四个答案一个不少。**字典级的反演**由 `tests/logic/test_permissions.py::test_role_sets_match_the_matrix_columns` 守着，不在这里重复。⑥ **那条对照用例是端点侧的复述、不是第二份权威**（`design.md` D10 的 ④）：矩阵权威在 `tests/logic/test_permissions.py`（13 §2.2 逐行抄录的整张表 + 互为反演的断言）。**已核实它真的守着**，不是「据说有」：把 `INBOUND_OPERATE` 加进 `PLANNER` 集合 ⇒ 逻辑层 **3 例红**（含 `test_every_role_permission_pair_matches_doc_13_section_2_2[inbound.operate-planner-False]`），本文件这 1 例同时红 —— 故本条是引用而非重复劳动，它留着只为回答「读端点契约的人接着要问的那一问」（D10 把资源标识定成了 `inbound.operate`）。同一用例顺带对四个角色钉住 `engine.invoke` 恒 `False`（`AUTO_ONLY` + `check()` 里这条判断先于查表），即 D10 第 3 条在端点侧的直读。⑦ 用例加在同一文件、另起一节「8.4 认证与权限口径」（3 条用例 / 6 个参数化形态），并在模块 docstring 里说明本文件已覆盖 8.1~8.4；`_token_for(role)` 另建一个该角色的账号并签凭据（中间件**取凭据里的 `role`、回查库里的 `status`**，故两处都要给对）。全量 **769 passed / 5.61s**（本任务 +6 条 / +0.20s，`<5s` 这条目标线**连续三个任务被越过**，已再次上报）
- [x] 8.5 落规模上限：>50 单拒绝并提示拆分，**不静默截断**；空数组产出空 `plans`。验证：`tests/api/` 覆盖 51 单被拒、0 单空响应两条用例。**实施补记（2026-09-12）**：① 上限的判决落成 `app/api/routes/allocate.py` 的 `_require_batch_size(raw_ids)`（`ValidationBlocked` 422，`detail = {"submitted", "max"}`），在报文组内**排在最前**：组内次序不是任意的 —— 上限问的是「这一批能不能受理」，形状与重号问的是「里面哪一条不对」，整体的判决先于逐条的判决；整组仍在取数之前（8.1 立的原则）。② **本任务含一次删除，不是纯新增**：8.1 在 `BatchAllocateRequest.job_order_ids` 上留了 `Field(max_length=MAX_JOB_ORDERS_PER_BATCH)`，而 `MAX_JOB_ORDERS_PER_BATCH` 的 docstring 早就写着「**不**写成 `Field(max_length=...)`，判断落在路由层（tasks.md 8.5）」—— 两处自相矛盾，8.5 落地时按常量处那句话把字段上的约束删掉。理由两条：Pydantic 层的 422 是**另一种响应体**（FastAPI 默认 `{"detail": [...]}`，没有 `error` / `message`），本端点把领域错误统一在 `errors.error_body` 上；且文档要的是「拒绝并**提示拆分**」，通用文案不会叫调用方去拆分。**它留着的危害恰恰是不报错** —— 超限照样被拒，只是形状不同；本任务用例**首跑红在 `KeyError: 'error'`**（RED 阶段抓的就是这一行）。③ **判据宽度 = 原始条数，不是去重后的条数**（本条唯一的取舍，两种读法都自圆其说，故理由写死）：`_requested_ids` 会去重，若先去重再判长度，一份 60 条带 10 条重号的请求会以「实为 50 条」被放行 —— 那依然是**静默削减**，只是削减发生在去重那一步；去重是防「同一张单被扣两次」的**正确性**检查，不该顺带承担规模上限。规格措辞也站这边（「请求提交了 51 条单据号」）。④ **同一个 422 下有两条路径，故用例断言 `detail` 而不只断言 `error`**：状态码相同的两条路径，靠状态码分不出来（把长度检查挪到取数之后，回的就是「未知单号」那个 422 —— 两者 `code` 都是 `validation_blocked`）。⑤ **空数组**：产出空 `plans` / 空 `degraded_alerts`、零方案行、零回写，但仍是一次 200 —— `bulk_batch_no` 与 `snapshot_version` 照常回显（契约把两者定成必填，而「这一批用了哪一版快照」对一次空批同样可答）。**空批不消耗批次号**：D11 的 `NN` 是「当日**已有**号的数量 + 1」，是**计数**不是**计数器**，无号被领走则序号不前移，连调两次拿到同一串（D2 的直读，不是撞号 bug）。⑥ 用例 3 条、加在同一文件的新一节「8.5 规模上限与空批」。**任务书只点名两条用例，第三条（51 条里带一条重号 ⇒ 同样被拒且 `detail` 证明判的是原始条数）是为 ③ 那条取舍补的** —— 取舍由实现定下，就得有可执行的证据，否则下一个人只看到一行代码和一句注释，改动它不会红。⑦ **变异验证 4 处**（首跑即全绿，故照 8.2/8.3 的处置做变异）：去掉长度检查 ⇒ 2 例红；把检查挪到取数之后 ⇒ 2 例红；改判去重后的条数 ⇒ 1 例红（**恰是**钉这条取舍的那一例）；把空数组读成「全量」⇒ 1 例红（**恰是**空批那一例）。四处改回后 `diff` 确认与验证过的版本逐字相同（本轮全部用 `cp` 备份 + `newline=''` 写入，未再出现 8.3 那次的 CRLF 事故）。⑧ 全量 **772 passed / 5.86s**（本任务 +3 条；`<5s` 这条目标线**连续四个任务被越过**，已第四次上报）

## 9. 异常与边界

- [x] 9.1 权重缺失 ⇒ 整批阻断、不产出方案（`design.md` D3 的「权重」行）。验证：`tests/api/` 断言返回阻断类错误且 `RecommendationPlan` 零新增。**实施补记（2026-09-12）**：① 验证性任务（与 8.3 / 8.4 同类），**端点上没有为它加一行**：`load_weights` 在组装点被调一次（不是逐单调），抛出的 `BlockedMissingPrerequisite` 在引擎之前、在任何写入之前 ⇒ 409、零方案行、零回写 —— 「整批」是这个位置**自带**的性质，不需要额外写「整批」的判断。② **它与「因子级降级」必须分得开，这正是 9.x 系列存在的理由**：缺某个**因子**的取值时引擎照常出方案、只在理由里标降级（D8，9.3/9.5 各验一处）；缺**权重**时连「该按什么比例算总分」都不知道，没有可降级的目标 —— 按 0 权重或均分权重继续跑会产出一份看起来正常、背后没有任何人类批准过比例的方案。一处阻断、一处降级不是不一致，是两类缺席的性质不同。③ 造数用 `make_scenario` 的显式口径 `weights=None`（不建任何权重行），判的是 `configured == 0` 这一支；另一支（已配但都没到生效时间）的提示语不同（操作员的动作也不同：去配一版 vs 等它生效或改生效时间），由逻辑层的 `load_weights` 用例覆盖，不重造第二份。④ 断言取 spec 原话那句「权重配置未就绪」而**不逐字相等**：提示语里还带仓库号与排查方向，是给人读的，不是契约 —— 逐字断言会把「把提示语改得更清楚」变成一次需要改测试的返工。⑤ 用例加在 `tests/api/test_allocate.py` 的新一节，同时断言 5 张单三件全不动（状态 / 批次号）。⑥ **首跑即绿**（行为本就成立）⇒ 照 8.2/8.3 的处置做**变异验证**：让 `load_weights` 在无版本时**静默退回默认权重** ⇒ 该例红 —— 它正是 9.1 要防的那件事。改回后 `diff` 确认逐字相同。⑦ 全量 **773 passed / 5.4s**
- [x] 9.2 容量不足 ⇒ 走降级链而非报错（「降级不是异常」，用 `degraded` / `degrade_reason` 表达）。验证：`tests/logic/` 覆盖 `AC-002`（降级到次优巷道集，不崩溃、不丢弃）。**实施补记（2026-09-12）**：① 先核对既有覆盖面再动手，结论是**四分之三已在**：`AC-002` 在本阶段有四处可断言的面 —— 降级到次优巷道集（`aisles == ("07",)` + `stop_tier.label == "远巷道"`）、不崩溃（走到断言即无异常）、不丢弃（`failures == ()` 且该单在 `outcomes` 里）三处由 `test_a_full_preferred_tier_descends_to_the_far_tier_and_says_why` 断言；缺的是第四处 —— **这次下探被记录下来**。② 补在第四处，因为它是本节（D8）管的那条通道：**方案级降级** `degraded` / `degrade_reason`。新用例 `test_in_batch_exhaustion_descends_and_records_the_same_degradation` 走 `AC-002` 的**第二种成因**（近站台的 10 格被同一批先来的一张单吃满，而不是「一开始就只有 5 格」），并顺带断言先来的单**不被记成降级**（`stop_tier.index == 0` 且 `degraded is False`）—— 否则「某单降级了」这句告警级信号会被稀释成背景噪音。③ **为什么这一处需要独立用例**：`test_the_deduction_applies_to_the_next_queue_item` 只断言「第二单落到 `02`、`01` 不在其候选集里」；把方案级 `degraded` 恒置 `False`（下探照旧影响 `aisles`、只是不再记账）后它**照样全绿**，而现场表现是「推荐了远巷道但读不到原因」—— 「降级不静默」被违反而无一变红。故同一份事实两条用例：一条守「选对了巷道」，一条守「说了为什么」。**变异验证支持这个判断**：恒置 `degraded=False` ⇒ **只有**新用例红，旧那条仍绿 —— 缺口形状可复现，不只是推理。④ 「首期次优巷道集 = 档 2」登记在案：档 1（需 `distance_weight` 分档阈值）与档 3（溢出区未定义物理形态）空集但占位（D5），故下探今天必然一步从档 0 落到档 2；**这不是把「次优」定义成「远巷道」**，阈值一给，档 1 自动参与。⑤ 按 10.2 的前置要求，给既有那条用例的 docstring 补上 `AC-002` 编号与「四条断言各自对应 `20` 场景表哪一句话」的对照表（10.2 要「注释注明场景编号」，这张对照是它的前置）；两条用例的排序断言都改成**按单号取**而不是按下标取（`outcomes` 是队列序 = 优先级降序，与提交序不是同一个序）。⑥ 全量 **774 passed / 5.88s**
- [x] 9.3 四类输入**全空**的端到端形态：无 `AisleCap` / 无 `InventoryItem` / 无 `Material.abc_class` / 无 `AisleStation` ⇒ 逐单因子级降级 + 四级走尽失败，**不抛异常、不静默落位**。验证：`tests/logic/` 一条集成用例跑通该形态并断言失败原因可见（这是阶段三唯一能跑通的真实形态，见 `16` §394）。**实施补记（2026-09-12）**：① 验证性任务（与 8.3 / 8.4 / 9.1 / 9.2 同类），**`app/` 下一行为它改动**：四类输入全空时引擎的行为本就如此，缺的只是一条把它钉住的用例。② **任务书那句「逐单因子级降级 + 四级走尽失败」不能按字面在同一条单上同时成立**：四级走尽的单没有 breakdown（`AllocationFailure` 只有单号与一段文案），因子层根本没被调到 —— 故落地时把它拆成**四条通道**分别断言，并登记在 `design.md` D8 的补记里（那是本任务唯一的「设计文档改动面」）：`AisleCap` 缺失 ⇒ **逐单失败**（cap 行是候选集的构成条件，没它连候选都不是，四级链没有一档停得下来）；`Material.abc_class` 缺失 ⇒ **因子级降级**（`abc_factor`）；`AisleStation` 缺失 ⇒ **因子级降级**（`station`，判据在 `scoring.station_degrade_reason`，粒度是方案级）；`InventoryItem` 缺失 ⇒ **不是降级**（合法空档案，`existing` 取 `0.00`）—— 最后这一条是本次最容易被写错的地方：把它记成降级会让首期**每一张**单都带一条永不消失的理由（`factors.py` 第 1 条 / `InventoryProfile` 的 docstring）。③ **造数里快照行**在**：`16` §11.6 的三类文件独立导入，cap / 库存缺失 ≠ 没有快照；「无快照」那一形态归 9.5，且端点在进引擎之前就闸住（D10 补记 ② 的两层分工）。若这里也拿掉快照行，9.3 与 9.5 会退化成同一条用例，且那道端点闸门在逻辑层被绕过而无人察觉。④ 另有两条造数口径要记：ABC 从**物料行上读**（该列为 NULL），不写字面 `None`（后者与「用例忘了给」同形）—— 为此给 `_item` 添了 `material_abc_class` 参数（原来只有单据侧的 `abc_class`）；批号照给（由系统在入库单建立时生成，不属这四类输入），免得造数里混进一条与 9.3 无关的降级。⑤ 用例是**一条**（任务书要求），分两段：第一段断言失败通道（每张单都在 `failures`、`outcomes` / `alerts` 为空、四档都点名、成因是「无候选巷道」而不是「无可行容量」、文案含 `PENDING` / `plans` / 人工介入），第二段在**同一份造数**上直接问那三个函数 —— 它们问的正是分配器在那份入参上会问的同一批取数产物（`load_snapshot_index` / `load_station_weights` 都真跑），不是另造一份形态。⑥ 失败文案的断言与 5.3 的单测**有意重叠**：单测守措辞本身，本条守「真实取数路径上确实走到这段措辞」。**成因那一条尤其要守** —— 本场景四档都是「无候选巷道」，若实现把两级成因合流，操作员会被告知去腾容量，而真问题是补 cap 数据（两种成因的处置相反，D19）。⑦ **变异验证 5 处**（首跑即绿 ⇒ 照 9.1 / 9.2 的处置），各自红在**预期的那一行**：失败文案丢掉四档成因 ⇒ 红在「四档都点名」那条；两级成因合流（无候选 ⇒ 无可行容量）⇒ 红在同一条；`station_degrade_reason` 在主数据全缺时返回 `None` ⇒ 红在 ③ 那条；`abc_factor` 把未知当 C 类放行 ⇒ 红在 ② 那条；`existing_factor` 把空档案记成降级 ⇒ 红在 ④ 那条。还原后逐字核对相同。驱动脚本改成「注入一处 → 只跑这一条用例 → **立即**还原 → `==` 核对」的单文件形式（8.3 那次半改残留的教训），注入与还原都用 `newline=""` 读写。⑧ 全量 **775 passed / 6.80s**（本任务 +1 条）。`<5s` 这条目标线**连续五个任务被越过**，第五次上报。
- [x] 9.4 写库异常 ⇒ **整批整体回滚**，不留「一半有方案一半没有」的中间态。验证：`tests/logic/` 用注入式失败（如第 3 条单落库时抛错）断言事务前状态完整恢复（`design.md` D12）。**实施补记（2026-09-12）**：① **验证条件的位置按落点现实调整为 `tests/api/`**（本变更第二处同类调整，第一处是 8.4），理由写在用例 docstring 与 `design.md` 的 D12 补记里：`app/engine/` 按 D4 从不碰会话（扣减只落内存、`allocate_batch` 收快照副本），写库整段都在端点；而 `tests/logic/conftest.py` 的 `session` 夹具把用例包在一层外层事务里、内层用 savepoint，端点里的 `session.commit()` 只释放那个 savepoint、外层最后整体回滚 —— **在逻辑层写这条用例换个写法都会绿**，是一份假保证。真库里「有没有一半写进去」只有走真实会话边界才看得见（本文件从 8.1 起就是那套夹具）。与 8.4 的差别：8.4 是纯验证性的（产品代码一行未动），本条**也不是新增行为** —— 写循环 + 单次提交落地于 8.2 那一步，本条把它钉住。② **注入点选在模块全局的 `RecommendationPlan` 上**（计数工厂：前两条照常建真行、`add` + `flush` 都真的发生，第 3 条抛），**不制造真的库错**：要的是「写到第 3 条时异常」这个**位置** —— 主键冲突 / 磁盘满 / 连接断在回滚语义上是同一件事。这依赖一个实现事实：`allocate.py` 用的是模块全局里的那个名字（不是函数内重新 import），且该模块有 `from __future__ import annotations`，换名不影响任何注解的求值。③ **断言三条**：注入确实落在第 3 条（`attempts == 三条单号`）、`plan_rows() == ()`、三单 `PENDING` 且 `bulk_batch_no is None`。第一条是**防空转**的 —— 注入点若挪到第 1 条，后两条照绿（什么都没写进去），那时这条用例证明的东西是零。读库走 `factory` 开的新会话 ⇒ 只有已提交的写入可见，这正是「一半有方案、一半没有」在操作员眼里会呈现的样子。④ **变异验证 4 处**（首跑即绿 ⇒ 照 9.1 / 9.2 / 9.3 的处置）：提交挪进写循环（「一个事务」破了）、撤掉 `get_db` 的回滚**与**关闭（「异常即回滚」破了）、回写后立刻提交，三处都红在 `plan_rows() == ()`；把注入点挪到第 1 条则红在防空转那条。还原后逐字核对相同（`app/api/deps.py` 那处还原到与 HEAD 逐字相同，`git status` 里根本不出现）。⑤ **必须说清这条用例分不开什么**：单个事务里「前两行在不在」与「前两单的状态 / 批次号在不在」是**同一次回滚的两个现场**，制造不出「方案回滚了而回写留下了」的形态 —— 后两条断言是同因的第二现场，不是独立判据（回写本身发生没发生由 8.2 独立钉住）；且撤掉 `rollback()` 与撤掉 `session.close()` 在这条用例看来一样，故它钉的是「库里不留中间态」这个**可观察结果**，不是 `get_db` 里那两行的写法。⑥ **顺带登记一条形态**（不新增要求）：整体回滚对手工重试是静默的 —— 事后读库三张单都是 `PENDING`，与「今天没调过这个端点」不可分辨，差别只在调用方当时收到的那一次 5xx。本阶段没有批次级痕迹（无台账、无 `ImportSession` 行）；写路径若在阶段四搬进 `app/services/inbound.py`，届时决定是否补一条批次级记录（已写入 `design.md` D12 补记 ③）。⑦ 全量 **776 passed / 5.73s**（本任务 +1 条）。`<5s` 这条目标线**连续六个任务被越过**，第六次上报 —— 本次墙钟反而比上一任务低（6.80s → 5.73s），说明这几秒里**测量噪声与真实增量同量级**，不是单调恶化。
- [x] 9.5 快照缺失时的**入库**行为与出库**不同**：入库按因子级降级**不阻断**；出库的快照过期阻断属阶段四，不在本变更。验证：`tests/logic/` 断言无 `Snapshot` 行时分配照常完成且理由标注降级（防把出库口径误搬到入库）。**实施补记（2026-09-12）**：① **验证性任务**（与 8.3 / 8.4 / 9.1 / 9.2 / 9.3 / 9.4 同类），`app/` 下一行为它改动 —— 缺快照时引擎照常分配、三个库存因子降级，本就是 2.x 起点就落成的行为，缺的只是一条把它钉住的用例（与端点那道 409 关口**并存**，两层各管一半，见 `design.md` D10 补记 ②）。② **「无 `Snapshot` 行」在库层面做不到「照常完成」，这是本任务最需要说清的一件事**：`AisleCap.snapshot_id` 是**非空外键**（`app/models/linkage.py`）⇒ 库层面一版快照都没有，必然**连带没有 cap 行** ⇒ 候选集为空 ⇒ 每张单四级走尽（那正是 9.3 的第一类形态），端点上则由 8.x 的 `test_a_missing_snapshot_blocks_the_batch` 闸住。故 9.5 能验、且**只能在引擎层验**的那句话是「喂它一个缺席的库存索引，它照常分配、只在理由里记降级」，入口就是 D10 补记 ② 点名的 `load_snapshot_index(session, snapshot_id=None)` ⇒ `SnapshotIndex.absent`。造数因此**保留快照行 + cap 行**（cap 是候选集的构成条件，拿掉它测的就成了 9.3），只让**索引那一项**缺席：`state` 与 `snapshot` 在引擎签名里本就是两项，用例把这个差异摆出来。为此给共用的 `_allocate` 加了 `snapshot_id` 参数（哨兵默认值 = 取造数那一版；显式 `None` = 缺席），四个 `load_*` 照旧真跑，不手搓索引对象。③ **四条断言各钉一件事**：不阻断（`failures == ()` / `alerts == ()`、单子在 `outcomes` 里）、选道不受影响（仍是档 0 首选 —— 降级的语义是「这个因子不参与」，不是「降级链下探」）、**方案级没有降级**（`degraded is False` / `degrade_reason is None`，D8 两条通道不共用字段）、**因子级降级恰为三个库存因子**且文案含「无库存快照」。再加一条：降级因子**不进分解**（D9 第 1 条 —— 留在里面会让操作员看到三个值为空的因子，以为它们参与了打分）。④ **渲染那一半不在这里重验**：`payload_json.factor_degraded` 由 `reasons.build_reason_payload` **逐字取用** `outcome.factor_degraded`（7.1 的测试已钉），本节验的是它的唯一来源 —— 与 D9 补记里「7.1 只验得到引擎侧那一半」的切法一致。⑤ **变异验证 3 处**（首跑即绿 ⇒ 照前几任务的处置）：缺席索引被当成空档案（`SnapshotIndex.build(())`）⇒ 红在「三项降级」；`continuity_factor` 不认缺席（把它算成 `0.00`）⇒ 红在同一条；**因子级降级被顺手记成方案级降级**（`degraded=... or bool(factor_degraded)`）⇒ 红在「方案级不降级」那条。还原后逐字核对相同（`factors.py` 三处降级分支、`allocator.py:419` 均已在位）。最后一处正是 D8 要防的通道合流，也是四条断言里**唯一由 9.5 独自守住**的：9.3 的场景里方案级与因子级同时出现，两者分不开。⑥ **顺带订正一处编号引用**：「两层分工」登记在 `design.md` **D10 的补记 ②**，而 8.1 补记 ②、9.3 补记 ③ 与 `test_allocator.py` 的模块 docstring 原写「D9 的两层分工」（D9 是「推荐理由的组装边界」，会把读的人指向一个不看这里的地方）；三处已一并改为「D10 补记 ②」。⑦ 全量 **777 passed / 5.75s**（本任务 +1 条）。`<5s` 这条目标线**连续七个任务被越过**，第七次上报；本任务与上一任务之间墙钟在 5.39~6.80s 之间摆动，**摆动幅度大于单条用例的增量**，故这几秒更像是环境噪声而非单调恶化。

## 10. 基线层评测场景落地与登记

- [x] 10.1 在 `backend/tests/logic/` 落**评分正确性**基线层场景 `SC-001~006`，每条一个用例、注释注明场景编号与 `20` 的原表预期。验证：`python -m pytest tests/logic -m logic` 全绿；逐条对照 `20` §「1. 评分正确性」的预期列。**实施补记（2026-09-12）**：① **验证性任务 + 一次审计**（与 8.3 / 8.4 / 9.x 同类）：六条场景在阶段三推进中已按「哪一层的行为」逐条落位，本次做的是**逐条对照 `20` §六「1. 评分正确性」的预期列**，并补三处缺口。② **交付形态不是 `test_scenarios.py` 大杂烩**：场景编号在这套测试里是**追溯标签**，用例仍按层归位（`test_scoring.py` 四条 = `SC-001` / `002` / `004` / `006`；`test_factors.py` 两条 = `SC-003` / `005`）—— 否则同一条事实会在两个文件里各写一遍。归属表与理由落在 `test_scoring.py` 的模块 docstring 新增一节里（含 `SC-005` 为何有两处落点）。③ **缺口一 `SC-002`：原用例的输入与预期列都不符**。原写 `_weights(cap=0.0)`（预期列的输入列写的是 `W_ABC=0`），且只断「两条只差该因子的分解得分相同」—— 漏掉预期列的第二半「**其余因子仍生效**」。已改写为 `W_ABC=0` + 两条断言。**第二半不是凑数**：一个把六项取值全丢掉的实现（恒返回 `0.00`）让第一半照样成立，现场表现是理由卡六项全空、所有巷道同分、选道退化成「按 `aisle_no` 升序」。④ **缺口二 `SC-005` 的「因而得分更高」那半原本没有落点，且原引用是假的**：因子层那半在 `test_factors.py`（早已存在），但它的 docstring 把「总分更高」那一层指向 **`3.3 的 test_scoring.py`** —— 那个文件里根本没有这条用例。而它也**不能**落在那里：`score_aisles` 收的是**手写的分解**，在那儿断言「有既有库存的巷得分更高」等于把刚填进去的数按恒等式重算一遍，是一句恒真的同义反复。故新增 `test_allocator.py::test_sc_005_an_aisle_holding_the_material_wins_on_its_score`（跨「因子值 → 总分 → 档内比较 → 落位」四处口径），那处假引用改为指向它；文件 docstring 里「本文件验的是循环、不是算术」一节同时登记了这一处**有意的例外**。⑤ **造数把库存放在编号大的 `02`**：一个把因子值丢掉的退化实现会按 `aisle_no` 升序挑 ⇒ 选 `01`，与期望值**相反**。**变异 M6 证实这个设计是必要的**：把 `select_best` 换成「取编号最小的」⇒ 新用例红，而**既有的** `test_the_highest_scoring_feasible_aisle_is_selected` 照样绿（它期望的赢家恰好就是编号最小的那条，故分不开「按分挑」与「按编号挑」）—— 那正是这条新用例补上的盲区。⑥ **缺口三 `SC-003` 的「不读 PO 批号」**：预期列第二半原先只有注释、无断言。补的是**入参形状**断言（`{profile, aisle, order_batch_no}`，没有会话、没有单据对象），依据是 `16` A.4 的 PO 模版没有批号列。⑦ **逐条核对结论**：`SC-001` 两形态（`station` 参与 / 降级）各断「得分最高 + 入选」✓；`SC-004` 断闭区间判据（刚好装得下必须留下）+ 差一格掉出候选集 ✓；`SC-006` 三方对照（文档的 value / 文档的权重 / 文档的期望 `0.79` / `0.58` / `0.43`）并按 `17` §10.1 恒等式用 `Decimal` **独立复算** ✓。⑧ **顺带把一句恒真断言换成真断言**（`SC-005` 因子层用例末尾原为 `continuity_factor(...).term is not None`，不禁任何东西）：该造数里同物料跨 `01` / `02` 两巷，故 `continuity` 对两条巷**等分 `0.50`**，本用例的抬升来自 `existing`；`continuity` 真正拉开差距的是「物料**不在**该巷 ⇒ `0.00`」那一支（已由另两条用例覆盖）。这条改动同时把「预期列把 `SC-005` 记在 `continuity` 上、而实现里先动的是 `existing`」这处分歧写在案，不是悄悄改数。⑨ **变异验证 6 处**（首跑即绿 ⇒ 照前几任务的处置）：权重没进分子 ⇒ 红在 `SC-002` 第一半；零权重被误读成零评分 ⇒ **红在第二半**（第一半绿，正是它要防的形态）；`existing` 的既有板数读成 `0` ⇒ 因子层与链路层各红一条；`continuity` 的「不在该巷道」支改成 `1.00` ⇒ 红在链路层那条的取值断言；`batch` 顺手多收一个 `po_row` 入参 ⇒ 红在形状断言；`select_best` 退化成取编号最小者 ⇒ 见 ⑤。还原后逐字核对相同。⑩ `python -m pytest tests/logic -m logic` = **487 passed / 1.17s**；全量 **778 passed / 5.79s**（本任务 +1 条）。`<5s` 这条目标线**连续八个任务被越过**，第八次上报。
- [x] 10.2 在 `backend/tests/logic/` 落**分配合规**场景 `AC-001` / `AC-002` / `AC-004` / `AC-005`，注释注明场景编号。验证：同上命令；`AC-004` 以 `predicted_cross_aisle` 的**预演**形态验收（非落位后实测）。**实施补记（2026-09-12）**：① 与 10.1 同一次审计，结论不同：**四条场景共五处用例已全覆盖，本次 0 条新增**。逐条对照 `20` §六「3. 分配合规」的预期列：`AC-001`「A 类保留预留比例，慢流转品不占用」—— 因子/容量口径在 `test_reserved.py`（B / C 拿 `cap_usable` = 60 = **不占用**；A 拿总额 80 = **保留的那份本就是它的**），候选集形态在 `test_scoring.py`（70 格的 B 类 09:00 只能走远巷 `02`、A 类两条都行、18:00 后 B 类也可行）；`AC-002`「降级到次优巷道集，不崩溃、不丢弃」—— `test_allocator.py` 两条用例（两种成因）把四句预期逐一断到（降级目标 `aisles == ("07",)` + `stop_tier.label == "远巷道"`、走到断言即无异常、`failures == ()` 且该单在 `outcomes` 里）；`AC-004`「引擎输出跨巷道数 ≤5」—— `test_reasons.py` 断的**正是预演形态**（`predicted_cross_aisle.material == 3`、`threshold == 5`、`exceeded is False`），与任务书「非落位后实测」一致，另有「阈值读配置行而不是写死 5」一条；`AC-005`「提示人工干预，不静默落位到非法巷道」—— 文案含「人工介入」且四档点名（`test_degradation.py`）+ 失败记录**没有巷道字段**（`fields(failure) == {job_order_id, message}`，数据结构上的兑现，塞不进 `DegradedAlert.aisle` 的必填形状）+ 分配器层的失败通道（`test_allocator.py`）。② 本次唯一改动是**注释层**两处：`test_reserved.py` 的模块 docstring 补 `20` §六 `AC-001` 与 `tasks.md 3.1 / 10.2` 两行；`AC-001` 用例的 docstring 补上预期列**两半**的对照 —— 原来只写「慢流转品不占用」，而用例其实两半都断，注释里缺前半会让读者以为少断了一条（只断前半也拦不住「把预留池对所有角色都藏起来」那种实现）。`AC-004` 的预演口径与 `AC-005` 的「没有巷道」原已在 docstring 写明，未动。③ `AC-003`（同批跨巷道 ≤3）属**不适用**，由 10.3 登记，本节不重复。④ 全量 **778 passed / 5.79s**（本节 +0 条，与 10.1 同一次运行）。
- [x] 10.3 在 `tasks.md` 与 `proposal.md` 之外**显式登记本变更不适用**的规则项，并写明归属阶段：`AC-003`（同批跨巷道 ≤3 —— 本契约本期只给同物料口径，同批口径留待阶段四后验）、后验与 `KpiSnapshot` 聚合（F8 / F10，阶段四）、导入校验失败阻断（F1 / F2，阶段四）、出库快照过期阻断（阶段四）、`CL-001~006` 作业闭环与 `TR-001~005` 集中度趋势（需落位与台账，阶段四起）。验证：`openspec validate recommendation-engine` 通过；本节条目与 `proposal.md` 的「非目标」逐条对得上。**实施补记（2026-09-12）**：① 登记落在**本文件第 10 组末尾的「不适用登记」一节**（不是勾选框任务 —— 登的是「谁来做」，不是一个动作）：六条逐条写明**归属阶段**与**本变更的相邻已落部分**。那第三列是关键：只登记「不适用」会让读者读到「这块无人认领」，而其中每一条在本变更都有相邻的、**已验过**的那一半（例如 `AC-003` 的同批口径不适用，但同物料口径的预演 `predicted_cross_aisle.material` 已落）。② **没有静默删任何一条** —— rules 是 F1~F10 的全链口径，删掉等于把归属从文书里抹去（本文件第 1 行的排序说明原本就指向「第 10 组的不适用登记」，此前那节还不存在，属欠账）。③ **`proposal.md` 补两条「非目标」**，使两边逐条对得上：`AC-003` 的同批口径、`CL-001~006` / `TR-001~005` 两类场景（原先这两条只在任务书里出现过，`proposal.md` 的「非目标」没有 —— 只在前者登记等于让「本变更不做什么」取决于读者是否翻到任务书）。其余四条（后验/KPI、导入阻断、出库快照、落位与台账）`proposal.md` 的「非目标」原本已有，未重复添。④ 验证：`openspec validate recommendation-engine` 退出码 0（**不加 `--strict`** —— 中文规格会触发 RFC2119 假警报，见 `CLAUDE.md` 与本变更的既有处置）。⑤ 本节 **0 条测试**，全量不因此变化（见 10.1 的 778 passed / 5.79s）。
- [x] 10.4 更新 `CLAUDE.md` §11「当前状态」为阶段三（v0.3.0）实际完成的内容，并订正 §11 标题仍写着「（阶段一）」的问题。验证：`git diff CLAUDE.md` 只落在 §11；与 `00` §2.1 的版本表一致。**实施补记（2026-09-12）**：① 标题「（阶段一）」→「（阶段三）」；正文从「阶段一已收尾」改为**三阶段台账**（阶段一 / 阶段二 / 阶段二·三交界 / 阶段三 / 未完成）。`git diff` 的 hunk 全部落在 §11 内（行 293~331），32 insertions / 14 deletions；换行符仍是 LF（`grep -c $'\r' CLAUDE.md` = 0 —— 此前登记的「工作副本是 CRLF」已不复存在，见 11.x 的收尾核对）。② **订正两处失真**（不只是加新内容）：(a) 原「未完成（均为阶段二起）」把 `backend/scripts/init_db.py` / `seed_dev.py` 列为待办 —— 两者**已在阶段二交付**（文件就在 `backend/scripts/` 下），留着会让下一个人去找一个已经在那儿的文件；(b) 原「**阶段三**增量」一节的标题与内容对不上号（`InventoryItem.zone` 清退属阶段二/三交界，且该变更**未归档**），改为「阶段二/三交界」并注明归档状态。③ **数字都是实测**而非沿用：全量 778 passed / 约 5.8s、逻辑层 487、任务 34/40（`grep -c '^- \[x\]'`）。**这里记一个坑**：`-q` 已由 `pyproject.toml` 的 `addopts` 提供，命令行再传一次会变成 `-qq`，连 `N passed in Xs` 那一行都不打印 —— 看起来像「跑完了但没有统计」，本任务第二次踩到（第一次是 10.1 首次取数）。④ 与 `00` §2.1 的版本表一致：`v0.3.0` = 阶段三（`27` · 设计 `14`）6 因子引擎 + 批量分配 + 降级链 ✓；文中**没有**写「阶段三已打标 `v0.3.0`」—— 合并与打标是 `00` §2.3 标准流程的第 6 步，尚未发生（本变更仍在 `phase-3/engine` 上、尚未提交）。⑤ 顺带把两条**未了的收尾项**写进状态（不新增任务，只让它们可见）：`retire-zone-column` 变更未归档（分支已合并、已删）；两条**已合并**的本地分支仍在（`phase-2/data-model-permission`、`clarify/401-failure-semantics`）。⑥ 本节未跑测试（纯文档改动），全量数字取自 10.1 / 10.2 的同一次运行。

### 不适用登记（本变更范围外的规则项 —— 非任务，无勾选框）

rules 给的是 **F1~F10 全链**口径，本变更只占 F3 / F4（见文首的排序说明）。下面逐条登记**本变更不承接**的规则项、其归属阶段，以及**本变更里相邻的那一半**（第三列不是附注：少了它，「不适用」会读成「无人认领」）。

| 规则项 | 归属阶段 | 为什么不在本变更 | 本变更的相邻已落部分 |
|---|---|---|---|
| `AC-003` 同批跨巷道 ≤3（`20` §六「3.」） | 阶段四（F8 后验） | 需要落位实测与后验链路才谈得上验收；本阶段的跨巷道数只有**同物料**口径的**预演** | `AC-004` 的 `predicted_cross_aisle.material`（7.2）+ 阈值读 `CapacityConfig` 配置行 |
| 后验与 `KpiSnapshot` 聚合 | 阶段四（F8 / F10） | 依赖落位执行结果 | 本阶段只产出 `RecommendationPlan`，不产生任何 `Ledger` 记录（proposal.md「非目标」） |
| 导入校验失败 ⇒ 阻断导入 | 阶段四（F1 / F2） | 四类文件的导入管线不在本期 | 四类输入全空的形态由 9.3 钉住（`16` §394 的预期状态） |
| 出库快照缺失 / 过期 ⇒ 阻断 | 阶段四 | 出库是**只读派生**（F10），不触发评分引擎 | **入库**侧的快照缺席形态由 9.5 钉住；两者是两层、不互搬（`design.md` D10 补记 ②） |
| `CL-001~006` 作业闭环一致性（`20` §六「2.」） | 阶段四起（F7 / F8） | 需「落位确认 → 台账 → 出库 / 移库」的完整链路 | 本阶段的「人确认」只到端点授权（`inbound.operate`）；**无**二次确认卡、不写台账 |
| `TR-001~005` 集中度趋势（`20` §六「4.」） | 阶段四起（回归类） | 需跨周数据累积与「80% 拣货量」口径 | 集中度的**预演**只有 `predicted_cross_aisle` 一项（含超阈提示） |

## 11. 收尾

- [x] 11.1 复核事实来源文档的改动面（**三批**，改前均 `cp` 成 `.<时间戳>.bak`、改后均 `diff` 核对）：① 本变更开始前，`17` §4.2 / §10.1 / §10.7 与 `14` §3.4 步 5 四处；② 2026-09-11 按用户「品质库区从全部文档移除」的决定，`14` §3.4 步 1（删判据）、`16`（关键字段总表 + A.2/A.3 删行 + 两条编号说明）、`17` §3.3、PRD 数据来源表两行 —— 逐处见 `design.md` D7 的改动面表；③ 2026-09-11 实现期按用户「`aisles` 取单条」的裁决订正 `17` §10.1（示例 `["01","02"]` → `["01"]` + 字段语义行补并列规则，2 行；`.bak` = `17-数据模型设计.md.20260911-225527.bak`）—— 见 `design.md` D16。验证：对 `D:\成品库位智能推荐\产品设计\` 下的 `.bak` 做 `diff`，三批改动各自只落在预期行。**实施补记（2026-09-12）**：复核方式是把 `产品设计/` 下 19 份 `.<时间戳>.bak` 逐对 `diff -u`（每链都补到最后一份与正本对比），覆盖 8 份文档。① **任务书的「三批」是实做批次的口径，而 `17` 一链实有 6 段改动**：除 ①②③ 外，同一会话还落了三段订正 —— **④「批号生成时点」**（`14` §2 关键字段表 + §3.4 步 5 的 note、`16` 三处、`PRD` 批次因子数据来源行 + 更正说明 + 变更历史 v0.15、`08` 理由行、`09` 总览行、`17` §2.4 批次表与 §10.7「预测跨巷道」口径的 note；由 `.231803` → 各正本，`17` 侧为 `.231803` → `.232239`）、**⑤`21` 巷 `abc` 值自洽**（`value` 0.50 → 0.67、`scores["21"]` 0.38 → 0.43；`.232239` → `.234300`）、**⑥`priority.score` 1.42 → 0.70**（`.234300` → 正本，含 §10.7 的 `aisles` / `priority` 同步与 §10.1 字段语义行补 `score` 值域）。三段均 `cp` 了备份、均 `diff` 核对，改动面都只落在预期行 —— **故此处按 6 段记账，不把 ④⑤⑥ 折进「三批」**。② **③ 当时只改了 §10.1 的两行，§10.7 里的同名示例仍是 `["01", "02"]` / `1.42`，直到 ⑥ 才同步** —— 两节在同一条裁决上短暂不一致，⑥ 的订正说明已留痕并点明与 §10.1 同源；终态一致（且 §10.7 按 `plan_id` 下钻、不复制理由体，没有第三份副本会失配）。③ **两处配错的数值经复算确认自洽**：`station` 降级 ⇒ 分母 `0.80`；`01` = (0.25×1.00 + 0.20×0.72 + 0.15×0.60 + 0.10×1.00 + 0.10×0.50) ÷ 0.80 = 0.7925 → `0.79`，`02` = 0.464 ÷ 0.80 = `0.58`，`21` = (0.25×0.67 + 0.20×0.90) ÷ 0.80 = `0.43`（旧的 `0.38` 与旧的 `0.50` 也彼此自洽 —— ⑤ 改掉的是「`value` 与同行 `note` 的 2/3 不自洽」，不是算术）；`priority.score` = (0.80 + 1.00 + 0.30) ÷ 3 = `0.70`。④ **`16` A.1 的「库区号」行与它那条编号说明没有对应 `.bak`**（`.220406` 备份里就已有那条说明）—— 该处移除**早于本变更**，`retire-zone-column` 的 `proposal.md` 已登记为「早已完成的另一件事」、其 `tasks.md` 1.1 只复核未再改，故不属本任务的证据缺口。⑤ 残留扫描与 `retire-zone-column` 1.1 一致：正本里「库区」只剩 `16` 三处编号说明（移除本身的留痕）与 `PRD` / `03` / `04` 的口语用法（指两个 WMS 仓库代码）。⑥ `13.143242`（`account_status` 加 `rejected`）、`17.143242` → `.213601` 与 `00-总体开发方案.md.bak-20260911`（`env` scope）分别是阶段二的 401 变更与阶段一的备份，不在本变更的改动面内
- [x] 11.2 核对**两项登记项均已了结**（二者均在 apply 之前就落定，本任务只作归档前的复核）：
  - **① 已了结** —— 用户 2026-09-11 决定「品质库区」从全部文档移除，改动已落并含 `.bak` 与 `diff` 核对（`design.md` D7 的改动面表）；连带发现 `InventoryItem.zone` **列**待独立清退，已另立 `retire-zone-column` 变更（**不由本变更夹带**）。
  - **② 已了结** —— 用户 2026-09-11 决定「订正」，已落 `specs/permission/spec.md` 的 `MODIFIED Requirements`，订正 `自动写权限封闭` 的口径（实质未变，见 `design.md` D10）。
  - 验证：`openspec validate recommendation-engine` 退出码 0；`openspec/specs/permission/spec.md` 的 `自动写权限封闭` **仍是旧措辞**（delta 未同步属正常），而 `openspec/changes/recommendation-engine/specs/permission/spec.md` 存在且含**两条原场景 + 一条新场景**；**归档时**确认同步后主规格正文已换成「以 `ledger` / `engine` 为目标资源」的资源级口径、且两条原场景一条不少
  - **实施补记（2026-09-12）**：① 品质库区侧 —— 11.1 的逐链 `diff` 已确认 `14` / `16` / `17` / `PRD` / `08` / `09` 六份正本零残留、`.bak` 与 `diff` 齐全（唯一例外是 `16` A.1 那处**早于本变更**的移除，`retire-zone-column` 的 `proposal.md` 已登记为「早已完成的另一件事」）；连带发现的 `InventoryItem.zone` **列**确已另立 `retire-zone-column`（该变更尚在进行中、未归档），本变更未夹带。② 订正侧 —— `openspec validate recommendation-engine` 退出码 **0**（13 条 RFC2119 警告，全是中文规格的假警报，与既有记录一致）；主规格 `自动写权限封闭` 仍是**旧措辞**（「不得通过手动入口触发台账写入或引擎调用」「不存在面向角色的手动触发端点」），delta 未同步属正常；delta 的 `MODIFIED` 携带**整条需求** —— 两条原场景（四角色均无台账写权限 / 引擎调用无手动入口）一条不少，新增第三条（业务动作间接触发引擎不算手动入口）。③ 归档时的复核（同步后主规格口径与场景完整性）不属本任务，指针已写在本任务验证行里，此处不重复
- [x] 11.3 全量回归 + `openspec validate recommendation-engine`（**不加 `--strict`** —— 中文规格会触发 RFC2119 假警报）。验证：`python -m pytest tests/` 全绿；`openspec validate` 退出码 0 且带 `✗` 的失败为 0。**实施补记（2026-09-12）**：全量 `PYTHONUTF8=1 .venv/Scripts/python.exe -m pytest -c pyproject.toml tests --tb=short`（cwd `backend/`）= **778 passed / 5.55s**、退出码 0（142 条 warning 全为 `starlette.testclient` 的 cookies 弃用提示，与本变更无关）；`openspec validate recommendation-engine` 退出码 **0**、`✗` **0** 条、`⚠` **13** 条（逐条都是中文需求触发 RFC2119 的假警报 —— 这正是任务书要求**不加 `--strict`** 的原因：加了会把警告升级为 failure、报 `0 passed`）。两处记账：① 命令刻意不带 `-q` —— `pyproject.toml` 的 `addopts = "-q --tb=short"` 之上再叠一个 `-q` 会变成 `-qq`，把 `N passed` 那一行也吞掉（该陷阱在 10.4 已记录）；② L1 门禁的 `<5s` 预算仍是既有问题（`l1-budget-pressure` 登记的 5 项超支主因），本变更未使其缓解，也未新增超支项 —— `-m logic` 选中的 487 条单独跑只需 1.26s，可见超支不在引擎这一层
