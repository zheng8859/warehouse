# 全链路验收签字（阶段八 · `32` 号 Step 4）

执行时间：2026-09-16 · 分支 `phase-8/ship` · 后端单进程 `http://localhost:8000`（SQLite WAL）
执行人：E0764（Claude Code 辅助）· 依据文档 `32-全链路验收与审查.md`

## 一、执行摘要

| 项 | 值 |
|---|---|
| 日期 | 2026-09-16 |
| 分支 / 版本 | `phase-8/ship` → 候选标签 `v1.0.0-rc.1` |
| 通过数 | 页面 8/8 · 安全 A · API 1457/1457 · Evals Go · 旅程 4/4 |
| 遗留 | **无阻断项**。Step 1/2 发现的 3 项（2 中移动端溢出 + 1 低 favicon）、审计 2 项（1 中 XSS + 1 低 favicon）已在 Step 4 全部修复并复测通过 |

Evals 三层（`run_evals --tier all --compare evals/baseline.json`）**Go（退出码 0）**：
L1 单元 31 / L2 集成 32 / L3 质量 15 / 冷路径观测 1，P0 护栏（台账完整性 · 决策可追溯 · 出域）全过，
基线对比三档均 `[记录]`（零劣化）。

## 二、分类通过率

| 分类 | 结果 | 通过率 / 结论 |
|---|:--:|:--:|
| 页面 | 8 功能页加载 / 标题 / JS 异常 / 失败请求全部通过；移动端 375×812 无横向溢出 | **8/8 = 100%**（≥90% 达标）|
| 安全 | 0 Critical · 0 High · 出域=0；1 中 XSS + 1 低 favicon 已修复 | **A** |
| API | `pytest tests/` 1457 passed / 0 failed（401/403/422/404 全矩阵 + 端点行为）| **100%**（≥95% 达标）|
| Evals | L1 31·L2 32·L3 15·观测 1 全过，P0 护栏全过，基线零劣化 | **Go**（≤5% 劣化）|
| 旅程 | 仓管员 / 主管 / 计划员 / 管理员 四条旅程走通 | **4/4 = 100%** |

> L3 默认口径为 15 道（`l3 and not observation and not slow`）：集中度 5 + 移库 4 + 性能基线 2（快）
> + KPI 基线 4；另 2 道性能基线为 `slow` 慢测、1 道冷路径为 `observation`，按 `30` 号 D8 独立统计，
> 不进 L3 通过率分母 —— 无用例被删除，`--run-slow` 时 L3 为 17 道。

## 三、发布建议

**可以发布。**

验收质量门禁（`32` 号）逐项满足：8 页无致命缺陷 ✅ · 安全无 Critical/High 且出域=0 ✅ ·
Evals 无 >5% 劣化 ✅ · 冷路径一键关闭 ✅。Step 1/2 的中/低发现已闭环修复并复测，无阻断项。

---

*本文由 `32` 号 Step 4 生成；Step 1/2 原始发现与修复记录见
`acceptance-checklist.md`、`reports/security-audit-report.md`。*
