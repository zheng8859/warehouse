# 换行符统一为 LF。
#
# 为什么需要显式钉住：本机 core.autocrlf=true，若不声明，检出内容会被转成 CRLF，
# 而仓库中存的是 LF —— 换一台机器（或换一个 autocrlf 取值）就产生整文件级的假 diff，
# 掩盖真实改动。本项目要求"同样输入必得同样输出"，换行符不应随机器而变。
#
# 另一层原因：sh 解析含 CRLF 的脚本会以 "bad interpreter" 一类错误直接失败
# （CR 被带入解释器路径）。本仓库当前无 *.sh，* text=auto 已覆盖未来的脚本文件，
# 因此不再罗列 *.sh / *.bat 之类的分类规则 —— 那属于自说自话的冗余配置。
#
# 注：本文件只影响之后检出/提交的内容，不会回写已在工作区的文件。
# 若工作区仍是 CRLF，删掉重新检出即可。
* text=auto eol=lf
