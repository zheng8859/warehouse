# CONTEXT.md

成品库位智能推荐 —— 核心领域术语表。

**用途**：让代码、测试、规格、提交信息说同一种语言。写模型、命名变量、写测试用例名、
写 spec 时，术语一律以本文件为准；本文件没有的术语，先去设计文档确认再新增。

**事实来源**：`D:\成品库位智能推荐\产品设计\` 下的设计文档。每条术语后的括号标注出处
（如 `17 §2.1`）。术语的定义与设计文档冲突时，**以设计文档为准并回来修正本文件**。

**命名约定**：实体名 PascalCase（`JobOrder`），字段/枚举值 snake_case 或本文件标注的取值。
枚举的权威定义在 `backend/app/core/enums.py`，权限矩阵在 `backend/app/api/permissions.py`。

---

## 一、主数据（Master Data）

系统的骨骼。所有容量与落位计算沿「仓库 → 巷道 → 库位」这条链聚合。

| 中文 | English | 一句话定义 |
|---|---|---|
| 仓库 | Warehouse | 数据的顶层过滤维度；首期单厂 `GTJ10036`，全部实体携带仓库号（`17 §2.1`） |
| 巷道 | Aisle | 库位号前 2 位；是 cap 与全部「巷道级因子」的聚合单位（`17 §2.1`） |
| 库位 | Location | 6 位编码：`010104` = 巷道 01 / 层列 01 / 格 04；**按文本读取，不可数值化**（`17 §2.1`） |
| 库区 | Zone | 库存分布上的分区（如品质库区）；归入「可行巷道集」的匹配条件之一（`14 §3.4`） |
| 格 | Slot | cap 的计量单位；「巷道总格数」是物理容量上界（`16 §6.1`） |
| 站台 | Station | 出库站台；「就近站台」是 6 因子之一（`17 §2.2`） |
| 巷道-站台主数据 | AisleStation | 巷道↔就近出库站台↔距离权重；**当前待业务方补充导出**，未导出前该因子降级（`17 §2.2`） |
| 物料 | Material | 料号 + 品名 + ABC 分类 + 箱规/板规（`17 §2.3`） |
| 物料编码 / 料号 | Material Code | 物料在系统内的唯一标识；卡片与方案表展示序第 2 列（`17 §2.3`） |
| 物料描述 | Material Description | 品名；展示序第 3 列（`17 §2.3`） |
| 箱规 / 板规 | Packing Spec | 「数量 → 板数 → 占用格数」的折算依据，按单厂换算规则内置（`17 §2.3`） |
| ABC 分类 | ABC Class | `A` / `B` / `C`；**决定预留池使用权与队列排序优先级**（`17 §九`） |

---

## 二、物料批次（Material & Batch）

物料 × 批次的二维分布，是「集中就近」的度量对象。

| 中文 | English | 一句话定义 |
|---|---|---|
| 批次 / 批号 | Batch | 入库过账时生成（PO 不含批号）；**移库不改批号**，仅调整所在巷道（`17 §2.4`、`15 §10.4`） |
| 生产日期 | Production Date | 批次属性，随库存快照带入（`17 §3.3`） |
| 库存分布 | InventoryItem | 快照明细行：快照版本 × 库位 × 料号 × 批号 × 数量；既有库位/批次因子的唯一输入（`17 §3.3`） |
| 库存状态 | Item Status | 合格 / 待检 / 冻结…**以源数据取值为准**，非封闭枚举（`17 §九`） |
| 收拢 | Consolidation | 把同一物料/批次的分散库存聚到少数巷道；移库作业的目的（`15 §2.3`） |

---

## 三、作业交易（Job Transactions）

「作业单 → 推荐方案 → 台账 → 后验」四步闭环。

| 中文 | English | 一句话定义 |
|---|---|---|
| 作业单 | JobOrder | 三类作业的统一载体，以 `job_type` 区分；一个批量批次共用一张表（`17 §4.1`） |
| 作业类型 | Job Type | `INBOUND` / `OUTBOUND` / `RELOCATE`（入库 / 出库 / 移库）（`17 §九`） |
| 作业状态 | Job Status | `PENDING`→`PLANNED`→`CONFIRMED`→`EXECUTED`→`VERIFIED`，另有 `REJECTED`/`CANCELLED`（`15 §3.1`） |
| 业务单号 | Business Doc No. | 入库 = PO 号，出库 = DO 号，移库 = 移库任务号（`17 §4.1`） |
| 单据号 | Doc No. | 卡片与方案表展示序**第 1 列**（`17 §2.3` 注） |
| 批量批次 ID | Batch Run ID | 一次批量生成一个批次，便于整体追溯（`17 §4.1`） |
| 推荐方案 | RecommendationPlan | 每单的候选巷道集 / 顺路取顺序 / 目标巷道 + 6 因子分值 + 降级标记（`17 §4.2`） |
| 候选巷道集 | Candidate Aisles | 入库推荐产物：可行巷道集按 6 因子排序后的结果（`14 §3.4`） |
| 顺路取顺序 | Pick Path Order | 出库推荐产物：按巷道聚合既有库存得出的拣货路径（`17 §10.2`） |
| 目标巷道 / 主巷道 | Target Aisle | 移库推荐产物：收拢的目标巷道（`17 §4.2`） |
| 库位族 | Location Family | 巷道确定后进一步推荐的库位集合（P0-F6）（`14 §五`） |
| 逐单处置 | Disposition | `ACCEPT` / `TUNE` / `REJECT`（接受 / 微调 / 驳回），逐单确认（`17 §九`） |
| 确认卡 | Confirmation Card | 写操作前的二次确认；**未确认不产生台账**（`13 §6.3`） |
| 台账 | Ledger | 三类合一、以 `ledger_type` 区分；由引擎/作业流**自动写**，无任何角色可手动写（`17 §4.3`） |
| 后验 | Verification | 执行后比对实际值与目标阈值，判定达标/偏离（`17 §4.4`） |
| 后验结果 | Verify Result | `PASS` / `DEVIATION`（`17 §九`） |
| 偏离批次 | Deviation | 后验不达标标记；成因分类（新入库收拢不达标 / 历史库存拖累）**决定处置路径**（`17 §4.4`） |
| 增量维护 | Incremental Update | 台账驱动的 cap 过程值：入库↓ / 出库↑ / 移库转移（`16 §6.3`） |

---

## 四、权限认证（Auth & Access Control）

| 中文 | English | 一句话定义 |
|---|---|---|
| 角色 | Role | 4 类，取值 **lowercase**：`warehouse_keeper` / `planner` / `supervisor` / `admin`（`17 §九`） |
| 仓管员 | Warehouse Keeper | 执行落位与出库作业的角色（`13 §一`） |
| 计划员 | Planner | 制定与调整落位计划的角色（`13 §一`） |
| 主管 | Supervisor | 例外处置与**权重配置**范围的角色（`13 §2.2`） |
| 管理员 | Admin | 账号管理与系统配置的角色（`13 §一`） |
| 账号 | Account | 用户名全局唯一 + 密码哈希 + 角色 + 状态；v1 本地账号，SSO 为路线图（`17 §6.1`） |
| 账号状态 | Account Status | **lowercase**：`pending`（待激活）/ `active` / `disabled`（停用即凭据失效）（`17 §九`） |
| 权限 | Permission | `resource.action` 形式的 17 项标识，如 `ledger.write`、`engine.invoke`（`13 §2.1`） |
| 权限矩阵 | ROLE_PERMISSIONS | 4 角色 × 17 权限的目标模型；**v1 未实现细粒度 RBAC**，矩阵定义见 `app/api/permissions.py`（`13 §2.2`） |
| 自动权限 | AUTO_ONLY | `ledger.write` / `engine.invoke` —— **不向任何角色开放手动入口**，仅引擎/作业流可触发（`17 §4.3`） |
| 会话凭据 | Session Token | JWT（HS256），claims = `user_id`/`role`/`status`/`iat`/`exp`/`warehouse_id`；8 小时一班次（`13 §7.2`） |
| 无状态会话 | Stateless Session | 无服务端 Session、无黑名单、无刷新；**吊销 = 置 `status=disabled`**（`13 §8.3`） |
| 三层权限检查 | Three-Layer Check | ① 认证中间件（v1，401）② PermissionChecker（路线图，403）③ 操作确认（v1，前端）（`13 §六`） |
| 最小可见原则 | Least Visibility | 角色只见其职责所需的数据范围（`13 §4.1`） |
| 数据安全边界 | Data Boundary | 核心链路数据不出域；脱敏后的冷路径出域允许（`13 §4.3`、`10 §七`） |
| 审计 | Audit | 完整审计日志与即时阻断为**路线图**；v1 靠台账 + 后验 + 操作记录满足追溯（`17 §6.3`） |

---

## 五、度量（Measurement）

容量（cap）与评分这两类「算出来的数」。

| 中文 | English | 一句话定义 |
|---|---|---|
| 快照 | Snapshot | 一次导入建立的库存基准；**版本化、归档不删除**，是 cap 的**权威**来源（`17 §3.2`） |
| 导入会话 | ImportSession | 一次导入批次的完整生命周期（文件清单 / 校验结果 / 分流去向 / cap 重算）（`17 §3.1`） |
| cap | Capacity | 巷道可用格数；分三个口径，见下（`16 §6.1`） |
| `cap_total` | Total Capacity | 巷道总格数 − 已占格数（`16 §6.1`） |
| `cap_reserved` | Reserved Capacity | `cap_total` × 预留比例（默认 40%），**仅近站台巷道**（`16 §6.1`） |
| `cap_usable` | Usable Capacity | `cap_total` − `cap_reserved`；**非 A 类只能用这个**（`16 §6.1`） |
| 预留池 | Reserve Pool | 近站台巷道为 A 类预留的容量；**非 A 类不得占用**（`16 §6.1`） |
| 超时释放 | Timed Release | 预留池到配置时点（默认 18:00）未被 A 类用完即自动释放给 B/C 类（`16 §6.1`） |
| 基线建立 | Baseline Rebuild | 每次快照导入成功 → 按巷道**全量重算** cap，生成新基线版本（`16 §6.2`） |
| 漂移 | Drift | 全量重算值 − 增量累计值；超阈值告警并**以快照重算值为准**（`16 §6.4`） |
| 对账 | Reconciliation | 每次新快照导入即一次全量对账（`16 §6.4`） |
| 容量告警 | CapAlert | 负 cap / 超总格 / 增量失败 / 漂移超阈值（`17 §3.4`） |
| 6 因子 | Six Factors | ABC / 巷道 cap / 既有库位 / 站台就近 / 批次 / 连续性；**不新增、不删除**（`14 §四`） |
| 因子分值 | Factor Score | 6 因子加权得分；权重来自 `WeightConfig`（`17 §4.2`） |
| 可行巷道集 | Feasible Aisle Set | `cap 足够` ∩ `品质库区匹配` ∩ `巷道可用` ∩（非 A 类排除预留池）（`14 §3.4`） |
| 批量竞争分配 | Batch Competitive Allocation | 按优先级**贪心**逐单选最优巷道并扣减容量；全局最优为路线图（`14 §3.4`） |
| 优先级排序 | Priority Ordering | 「谁先挑」；6 因子决定「放哪好」，两者分层（`14 §3.2`） |
| 降级链 | Degradation Chain | `近站台 → 次近巷道 → 远巷道 → 溢出区`；**严禁静默失败**，每步记录降级原因（`14 §3.5`） |
| 降级原因 | Degrade Reason | 写入推荐理由 JSON；降级**不是异常**，不抛错（`14 §3.5`） |
| 溢出区 | Overflow Zone | 降级链的终点兜底区域（`14 §3.5`） |
| 推荐理由 | Recommendation Reason | 可追溯的评分与降级说明 JSON，是「决策权在人」的证据链（`17 §10.1`） |
| 热路径 | Hot Path | 评分/落位/执行/后验/台账 —— **禁止 LLM**，秒级、确定性、不出域（`10 §一`） |
| 冷路径 | Cold Path | KPI 解读/归因/调优/移库方案 —— **可用 LLM**，低频、脱敏后出域（`10 §一`） |

---

## 六、KPI

口径由 `18` 号定义，闸门由 `20` 号定义 —— **口径与闸门分离**。

| 中文 | English | 一句话定义 |
|---|---|---|
| 拣货量加权集中度 | Weighted Concentration | 对一张 DO 按拣货量降序累加至 **80%** 所覆盖的巷道数；**v0.16 唯一统一验收指标**（`18 §1.3`） |
| N | Concentration N | 加权集中度的巷道数阈值，默认 5，可配（`17 §七`） |
| 同物料跨巷道数 | Same-Material Cross-Aisle | 同一物料号在库内占据的巷道数；目标 ≤5，基线 11.7~22.4（`18 §1.3`） |
| 同批跨巷道数 | Same-Batch Cross-Aisle | 同一批号在库内占据的巷道数；目标 ≤3，基线 5.8~21.8（`18 §1.3`） |
| 单张开单跨巷道数 | Single-DO Cross-Aisle | **已降级为参考监控值，不作硬验收**（基线 6~8）（`18 §1.3`、`18 §8.2`） |
| 推荐采纳率 | Adoption Rate | 「接受 / 微调后确认」占总推荐数的比例；目标 ≥60%（`18 §1.3`） |
| 后验达标率 | Verification Pass Rate | 同物料≤5 / 加权集中度 80%≤N / 同批≤3 **三项同时达标**的批次占比（`18 §1.3`） |
| 落位准确率 | Placement Accuracy | 实际落位与推荐库位族一致的比例；目标 ≥99%（`18 §1.3`） |
| 北极星 | North Star | **周有效收拢入库批次** —— 一周内被采纳且后验同物料跨巷道 ≤5 的入库批次数（`18 §1.3`） |
| 指标快照 | KpiSnapshot | 按统计周期存 KPI 值的实体；本系统只存实体，口径由 18 号定义（`17 §5.1`） |
| 达成率 + 趋势 | Attainment & Trend | Go/No-Go 用「达成率 + 趋势」而非单点判定（`18 §8.1`） |
| 反指标 | Anti-Metric | 警惕虚高：采纳率虚高但后验不达标 / 后验达标率下滑 / 推荐被绕开 / 落位事故（P0 必须为 0）（`18 §1.3`） |
| Go/No-Go 闸门 | Go/No-Go Gate | 集中度达成率 ≥70% 且趋势向好 + 护栏全过 → Go（`20 §7.5`） |

---

## 七、配置（Configuration）

配置**集中管理，改一处即全局生效**；全部版本化，历史版本保留可回滚。

| 中文 | English | 一句话定义 |
|---|---|---|
| 权重配置 | WeightConfig | 6 因子权重 + 版本号 + 生效时间 + 变更人；变更后**下次评分生效**（`17 §七`） |
| 容量与阈值配置 | CapacityConfig | 预留比例 / 释放时点 / 集中度 N / 跨巷道阈值 / cap 漂移阈值（`17 §七`） |
| 字段映射 | FieldMappingConfig | 源列名 → 本系统字段 + 必填 + 校验规则；**按单厂内置，不提供可视化配置**（`17 §七`、`16 §10.2`） |
| 问句模板 | PromptTemplate | 对话台 L2 常用问句 chip 的数据来源，映射到 4 类冷路径能力（`17 §七`） |
| 预留比例 | Reserve Ratio | 近站台预留占 `cap_total` 的比例，默认 **40%**（`16 §6.1`） |
| 释放时点 | Release Time | 预留池自动释放的时刻，默认当日 **18:00**（`16 §6.1`） |
| 漂移告警阈值 | Drift Alert Threshold | 重算值与累计值差异的超限门槛，默认 1%（`16 §6.5`） |

---

## 八、对话台（Conversation Console）

**确定性操作入口，不是自主 Agent** —— 不做长期记忆，不做跨会话画像（`17 §八`）。

| 中文 | English | 一句话定义 |
|---|---|---|
| 对话式操作台 | Conversation Console | 承载推荐/入库/出库/移库/KPI 查询的确定性指令入口（`14 §7.1`） |
| 两层指令模型 | Two-Tier Command Model | L1 确定性指令 + L2 复杂问句（`15 §8.1`） |
| L1 确定性指令 | L1 Deterministic Command | 参数化语句，本地规则解析，**不走 LLM**（`15 §8.3`） |
| L2 复杂问句 | L2 Complex Query | 自然语言，**意图识别走外部 LLM**（脱敏后）（`15 §8.2`） |
| 意图识别 | Intent Recognition | 把自然语言映射到确定性功能；技术分层可渐进（`14 §7.3`） |
| 常用问句 chip | Prompt Chip | 点击即用的预置问句，回答「点更好还是输入更好」（`15 §8.4`） |
| 写操作确认卡 | Write Confirmation Card | 对话台触发写操作**强制**弹确认卡，未确认不执行（`14 §7.4`） |
| 对话上下文 | ConversationContext | 短期记忆：当前页面上下文 + 最近问句 + **待确认写操作**；超时即清理（`17 §八`） |
| 数据脱敏 | Desensitization | 不传客户名/价格/供应商/配方/真实产能/订单号；仅料号/库位/数量与聚合指标出境（`10 §七`） |
| 数据不出域 | No Data Egress | **核心链路出域 + 未脱敏出域 = 0**；脱敏后的冷路径出域允许且不计护栏失败（`10 §七`） |
| 成本护栏 | Cost Guardrail | 三道硬上限：单请求 token 上限 / 并发上限 / 月度预算熔断（`10 §七`） |
| AI 建议标注 | AI Notice | 所有 LLM 输出须标注「AI 建议，仅供参考，需人工核实，不自动执行」（`10 §七`） |

---

## 九、易混淆对照

写代码时最容易搞错的几组，单独列出来。

| A | B | 区别 |
|---|---|---|
| `cap_total` | `cap_usable` | 前者含预留池，**A 类**可用；后者已扣预留，**非 A 类**只能用后者 |
| 快照重算值 | 增量累计值 | 前者是**权威**（每次导入全量重算），后者是**过程**（台账驱动）；冲突时以快照为准 |
| 台账 | 后验 | 台账记「做了什么」，后验判「做得对不对」；台账是 cap 增量的**唯一来源** |
| 偏离 Deviation | 降级 Degradation | 偏离是**后验**结果（事后判定不达标）；降级是**推荐**行为（事前容量不足）。**降级不是异常**，不抛错 |
| 单张开单跨巷道数 | 加权集中度 | 前者已降为**参考监控值**；后者才是**统一验收指标**。不要拿前者当验收 |
| 同物料跨巷道 | 同批跨巷道 | 前者按**物料号**聚合（目标 ≤5），后者按**批号**聚合（目标 ≤3） |
| 作业单 JobOrder | 作业类型 JobType | 一张作业单有且只有一个类型；台账共用一张表靠 `ledger_type` 区分 |
| `JobStatus` | `ImportStatus` | 两套独立状态机，取值域完全不同，不要混用 |
| 巷道 | 库位 | 库位号 `[:2]` = 巷道；cap 与巷道级因子**只在巷道层聚合**，不落到库位 |
| 预留池 | 溢出区 | 预留池是**给 A 类留的**近站台容量；溢出区是**降级链终点**的兜底区域 |
| 热路径 | 冷路径 | 热路径禁止 LLM（确定性、不出域）；冷路径可用 LLM（脱敏后出域）。**判断标准是「机器算 vs 人读」** |
| 操作与确认记录 | 23 实体之一？ | **不是** —— 它是 `17 §4.5` 描述的可追溯链路，处置结果落在 `JobOrder` 上，**不要为它建模型** |

---

## 十、不存在的概念（避免误用）

- **板号级 / 批号级推荐** —— 不做，推荐粒度是物料号（`04` 边界）
- **长期记忆 / 用户画像** —— 不做，对话台是确定性入口非 Agent（`17 §八`）
- **实时接口 / 与 WMS 实时同步** —— 不做，仅文件级衔接（`19 §3.4`）
- **待检状态转换 / 品管联动** —— 不归本系统（`17 §九`）
- **A/B 测试 / 用户行为埋点** —— 不做，试点用前后对比（`20 §九`）
- **KPI 由 LLM 生成** —— 不做，KPI 走确定性三层聚合（`18 §8.4`）
