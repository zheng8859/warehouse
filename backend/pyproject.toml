[project]
name = "warehouse-slot-recommender"
version = "0.1.0"
description = "成品库位智能推荐 — 物料号级集中就近落位推荐引擎（单厂 GTJ10036 试点）"
# ★ 仅在 Python 3.14.4 上实测过；3.11~3.13 未验证。
requires-python = ">=3.11"
readme = "README.md"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q --tb=short"
# L1 单元须 < 5 秒（00-总体开发方案 §4.3 pre-commit 门禁）。
# 三层 TDD 的目录划分见 tests/models、tests/api、tests/logic。
markers = [
    "model: 模型测试（CRUD + 约束 + 默认值）",
    "api: API 测试（端点行为 + 认证 + 错误处理）",
    "logic: 逻辑测试（6 因子评分 / 批量分配 / 状态机 / cap 计算 / 降级链）",
    "l1: Evals L1 单元层（纯函数，确定性 100%，≥95%）",
    "l2: Evals L2 集成层（API 行为，≥90%）",
    "l3: Evals L3 质量层（验收口径 + 趋势判定，非百分比门槛）",
    "slow: Evals 慢测（性能基线，仅 --run-slow 时纳入）",
    "observation: Evals 冷路径观测（4 能力抽样，独立统计、不进 pass_rate）",
]

[tool.coverage.run]
source = ["app"]
omit = ["app/llm/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
]
