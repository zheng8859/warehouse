# 版本为 2026-09-11 本机实测版本（Python 3.14.4），非文档规定的版本 —— 设计文档未指定依赖版本。
# 改动前先跑 pytest 与 evals，再改这里的钉子。

# ---- 运行期 ----
fastapi==0.119.1
uvicorn[standard]==0.52.0
starlette==0.48.0
sqlalchemy==2.0.49
pydantic==2.12.3
pydantic-settings==2.14.0

# ---- 文件导入（19 号选型：Python venv + FastAPI/SQLAlchemy/pandas/openpyxl）----
pandas==2.3.3
openpyxl==3.1.5
python-multipart==0.0.27

# ---- 认证 ----
bcrypt==5.0.0
# 注意：不要引入 passlib。
# passlib 1.7.4 + bcrypt 5.x + Python 3.14 下调用 passlib.hash.bcrypt.hash()
# 会抛 ValueError（passlib 的后端探测触发 bcrypt 的 72 字节校验）。
# 详见 app/core/security.py。
#
# 也**不要引入 PyJWT / python-jose / authlib**（design.md D7）：
# 会话凭据是 HS256 单一算法的自实现，用标准库 hmac + hashlib + base64 + json。
# 通用 JWT 库必须支持多种算法（含 alg=none）才叫通用，而本产品只用一种 ——
# 自实现后「算法协商」这条路在结构上就不存在（alg 只被核对、不参与选择）。
# 原 PyJWT==2.13.0 钉子已在阶段二移除，守卫见 tests/logic/test_token.py 的 AST 检查。

# ---- 开发与测试 ----
pytest==9.0.3
pytest-cov==7.1.0
pytest-asyncio==1.3.0
httpx==0.28.1
pre-commit==4.4.0

# ---- 数据库迁移（开发阶段补充，设计文档未指定）----
alembic==1.19.0
